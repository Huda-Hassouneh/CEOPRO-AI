# CEOPRO AI — Pending Actions for Other Teams / Humans

Things discovered during AI/ML-track work that need a decision or action from **someone other than
the AI/ML track** — a different team, or a human with permissions this session doesn't have. Nothing
in this file gets fixed by AI/ML work; it's a visibility list so the right owner notices it.

**Convention:** unlike `AI_PROGRESS.md` (strictly append-only), rows here get their **Status**
updated in place once resolved — add a one-line resolution note rather than deleting the row, so
there's still a record of what happened. New items get added to the bottom of their section.

---

## Blocking (something can't be built until this lands)

| # | Item | Owner | Blocks | Status |
|---|---|---|---|---|
| 1 | Add `pgvector` extension + a chunk-storage table to schema | Infra/DB | Semantic (embedding/FAISS) retrieval for Phase 3 RAG Chatbot (spec §21) | 🟡 Schema part done (`rag_document_chunks` table with a `VECTOR(1024)` column, `CREATE EXTENSION vector`). `docker-compose.yml`'s `postgres` image was updated to try to fix deployability, but the new tag doesn't exist: `image: pgvector/pgvector:15-pgorg` — confirmed via `docker pull`: `not found`. The correct tag, confirmed by successfully pulling and using it in this session's own testing, is `pgvector/pgvector:pg15`. See item #17 for the full history of this specific line. Lexical (BM25) retrieval doesn't need any of this — already built/tested against existing tables. |
| 2 | Add Row-Level Security policies for tenant isolation | Infra/DB | Defense-in-depth on multi-tenancy (spec §10, §37) | 🟡 Policies landed 2026-08-07 (`ALTER TABLE ... ENABLE ROW LEVEL SECURITY` + `CREATE POLICY` on all 18 tenant-scoped tables) — **but confirmed by direct test that they currently provide no actual protection**: PostgreSQL exempts the table owner from RLS by default, and the app connects as `ceopro_admin`, the same role that created every table (per `.env.example`). Tested directly: connected as `ceopro_admin`, seeded two tenants' data, ran a query with **zero tenant filter** — it returned both tenants' rows. RLS as currently configured would not catch a missing-`WHERE tenant_id`-clause bug, which is the exact scenario it exists to guard against. Needs either a separate, lower-privileged application role (not the schema owner) that the app connects as, or `ALTER TABLE ... FORCE ROW LEVEL SECURITY` on top of what's there (forces the policy even for the owner) — the policies and `current_setting('app.current_tenant_id', ...)` plumbing themselves look correctly designed, it's specifically the connecting-role setup that makes them inert right now. |
| 3 | Add `currency_rates` table | Infra/DB | Cross-currency pricing logic (spec §9, §19) | ✅ Resolved 2026-08-07 — table landed in `init_schema.sql`. Not yet wired into `src/ai/pricing/` (still same-currency only); that's now unblocked AI/ML work, not logged as a pending action anymore. |
| 4 | `reviews`/`news_record`/`social_mention`/`extracted_entity`/`sentiment_results` tables don't exist | Infra/DB | NER persistence (spec §15) and sentiment analysis (spec §16) | 🟡 Partially resolved 2026-08-07 — `reviews` and `sentiment_results` both landed, which unblocks sentiment analysis entirely (source text + a place to write results). `extracted_entity`, `news_record`, `social_mention` are still missing, so NER persistence and news/social-based market intelligence remain blocked. |
| 5 | No real competitor price data or scraper running (`competitor_prices` table exists but is empty) | AI Market Scraper Service owner (per `DATA_OWNERSHIP_AND_CONTRACTS.md`) | Phase 6 Competitor Ranking (spec §20); Phase 5 Price Intelligence logic is now built and tested against seeded data (see `AI_PROGRESS.md`, `src/ai/pricing/`) but has nothing real to run against | 🟡 Open — AI/ML side is ready and waiting; the scraper itself is still the blocker |

## Needs a decision (not blocking yet, but ambiguous/contested)

| # | Item | Owner | Why it matters | Status |
|---|---|---|---|---|
| 6 | `src/infrastructure/messaging/ai_consumer.py` already consumes `market.intelligence.raw` and writes to `evidence_records` under infra's ownership — functionally overlaps with where a future AI market-intelligence module would live | Whoever owns both infra/messaging and the AI roadmap | Two systems risk double-writing `evidence_records` once that AI module is built | 🟠 Open — needs an explicit call, not resolvable by either side alone |
| 7 | `model_versions` has no `artifact_path` column, so trained model binaries can't be pointed at their MinIO location (`ceopro-ai-artifacts`) yet | Backend/DB | Small, additive schema change (`ALTER TABLE model_versions ADD COLUMN artifact_path VARCHAR(500);`) — low risk, just needs someone to run it | 🟡 Open — small ask, easy to unblock |
| 8 | `.github/workflows/staging-deployment.yml` builds/deploys `Dockerfile.ai` and a `docker compose ... ai` service — **neither exists anywhere in the repo, on any branch** | Infra/DevOps | CI will fail the first time this workflow actually runs end-to-end (currently only triggers on push to `main`) | 🟠 Open — discovered during this session's repo audit, not caused by AI/ML work |
| 9 | No root-level `requirements.txt`, but `Dockerfile.backend` runs `pip install -r requirements.txt` | Backend/Infra | Backend Docker build is currently broken | 🟠 Open — discovered during this session's repo audit |
| 14 | `products` table has no `cost`/COGS column | Backend/DB | Spec §19 calls for margin-based pricing guardrails; without a cost basis, `src/ai/pricing/guardrails.py` can only bound *how much a suggested price can move*, not *whether it stays profitable* — a materially weaker guardrail | 🟡 Open — small ask (`ALTER TABLE products ADD COLUMN cost NUMERIC(10, 2);`), same shape as item #7 |
| 15 | `src/ai/rag/data_access.py` only decodes MinIO objects as plain UTF-8 text — PDF/DOCX aren't extracted, though `MINIO_STORAGE_ARCHITECTURE.md` explicitly expects "supplier PDFs, business text files" in `ceopro-rag-knowledge` | AI/ML (self-flagged — not someone else's blocker, but noted here so it isn't silently assumed done) | Any uploaded PDF/DOCX in that bucket is currently invisible to RAG ingestion, not an error, just silently unmatched by any document row that references it | 🟡 Open — needs `pypdf`/`python-docx` added; not started |
| 20 | `docker-compose.yml`'s `grafana` service now references `${GRAFANA_ADMIN_USER}`/`${GRAFANA_ADMIN_PASSWORD}`, but `.env.example` was not updated to document them | Infra/DevOps | Minor — `docker compose` substitutes empty strings for undefined vars rather than failing, so this doesn't break the stack, but Grafana ends up with blank admin credentials by default and anyone following `.env.example` wouldn't know these vars exist | ⚪ Open — low priority, cosmetic/documentation gap |

## Security

| # | Item | Owner | Status |
|---|---|---|---|
| 10 | Hardcoded Postgres password fallback in `src/infrastructure/monitoring/watchdog.py` (`DATABASE_URL` default embedded a real-looking password in a public repo) | Whoever owns `watchdog.py` (Infra) | ✅ Resolved 2026-08-07 — `DATABASE_URL` now required (raises `RuntimeError` if unset), no hardcoded fallback. |
| 18 | **The exact same anti-pattern as #10 was reintroduced in a different file, the same day #10 was fixed.** `src/infrastructure/messaging/ai_consumer.py`'s `_resolve_db_url()` now falls back to hardcoded `POSTGRES_USER=ceopro_admin` / `POSTGRES_PASSWORD=SecurePassword2026` when `DATABASE_URL` isn't set — replacing what was previously (as of the commit right before this one) a `if not self.db_url: raise RuntimeError(...)` guard, which was removed. Separately in the same change: `_write_ingestion_staging_record` now inserts into `import_staging_rows` with a hardcoded `job_id = '00000000-0000-0000-0000-000000000000'`, but `import_staging_rows.job_id` has a foreign key to `ingestion_jobs(job_id)` (added in the same schema update that added `reviews`/`sentiment_results`) — an all-zeros UUID almost certainly doesn't exist there, so this insert will likely fail its FK constraint on every real message processed. Not verified end-to-end (this is infra's consumer, not run as part of AI/ML's test suite), but the FK relationship and the hardcoded UUID were both read directly from the current schema and code. | Whoever owns `ai_consumer.py` (Infra) | 🔴 Open — both parts (credential fallback removal, FK-violating insert) need a fix; flagging together since they landed in the same commit |
| 19 | `.github/workflows/staging-deployment.yml` has a new step ("Run Dynamic Artifacts Documentation Engine") with a hardcoded `JWT_SECRET` and `DATABASE_URL` (including a plaintext password) directly in the workflow YAML, instead of using `${{ secrets.* }}` the way every other credential in this same file does. The step also `import_module()`s `src.backend.main` and `src.ai.main` expecting a FastAPI `app` attribute on each — **neither module exists anywhere in the repo** (confirmed via `git ls-tree` on `origin/main`), so this step will always fail (it explicitly `sys.exit(1)`s on any import error). | Infra/DevOps | 🔴 Open — two separate issues in the same new step: hardcoded credentials in a YAML file (should use repo secrets), and a step that references source files that were never created. Currently moot in the same way as #16 (pipeline fails earlier at the `Dockerfile.ai` step, #8), but two more landmines waiting for whenever #8 gets fixed. |
| 16 | `watchdog.py` still doesn't parse as valid Python — **a different defect than originally reported**. The original PowerShell-heredoc-wrapper bug (`@'`/`'@ \| Out-File ...`) was fixed in a later commit (2026-08-07), but that same fix also deleted the module docstring's opening and closing `"""` lines, leaving the docstring text as bare top-level statements. Confirmed via `ast.parse()` on the actual git blob (not a local copy — an earlier check of a `PowerShell Out-File`-written copy falsely reported a BOM error that wasn't in the real file): `SyntaxError: invalid syntax` at line 2. Also confirmed via `git merge-tree` that this doesn't conflict with PR #3 (unrelated file), just still broken on `main`. | Whoever owns `watchdog.py` (Infra) | 🔴 Open — still won't import or run. **Currently moot in practice**: `staging-deployment.yml` fails earlier anyway, at the `Dockerfile.ai` build step (item #8), before it would ever reach the `python watchdog.py` step — but will matter the moment #8 is fixed, so still worth fixing rather than leaving as a second landmine. |
| 17 | `docker-compose.yml`'s `postgres` image, across three attempts now: (a) originally plain `postgres:15-alpine` — confirmed no pgvector support (`ERROR: extension "vector" is not available`); (b) changed to `pgvector/pgvector:15-pgorg` — **this tag doesn't exist** (`docker pull`: `not found`) and the same commit also stripped the `${POSTGRES_USER}`/`${POSTGRES_PASSWORD}`/`${POSTGRES_DB}`/`${MINIO_ROOT_USER}`/`${MINIO_ROOT_PASSWORD}` variable substitutions down to empty strings; (c) a later commit (same day) restored the `${...}` variable substitutions correctly, but the invalid `15-pgorg` tag is still there, unfixed | Infra/DevOps | 🔴 Open — the correct tag, confirmed working in this session's own testing (successfully pulled and used to load the real schema including RLS policies), is `pgvector/pgvector:pg15`. Until fixed, `docker compose up` can't pull the `postgres` image at all — this blocks the whole local dev stack, not just pgvector/RLS-dependent features. |

## Waiting on documents / approvals (not code)

| # | Item | Owner | Status |
|---|---|---|---|
| 11 | `AI_ENGINEERING_PLAN.md`, `AI_CONTRACT_CHANGES_AND_CLARIFICATIONS.md`, and the original `AI_PROGRESS.md` referenced by `AI_PLAN_AND_CONTRACT_UPDATES.md` (module-level detail, "Module E/J/K" references) don't exist anywhere in this repo's git history | Whoever authored `AI_PLAN_AND_CONTRACT_UPDATES.md` | 🟡 Open — acknowledged as "will provide later"; AI/ML work in the meantime is going directly off `MASTER_SPEC_v4.md` |
| 12 | `MASTER_SPEC_v4.md` and `AI_PLAN_AND_CONTRACT_UPDATES.md` are staged locally / pushed to `noorhassouneh-patch-1` but not yet merged to `main` (reported as pending approval, pusher lacks `main` permissions) | Whoever holds merge rights on `main` | 🟡 Open — not blocking AI/ML work (content already available on the patch branch), but other teams won't see these docs until merged |
| 13 | Orange infrastructure environment (CPU/GPU availability, on-prem vs. cloud) not yet confirmed (spec §28) | Whoever owns the Orange deployment relationship | ⚪ Deferred — confirmed CPU-only / no GPU "for now" for this track; revisit if/when GPU becomes available |

---

## How to update this file

- Resolving an item: change its Status cell (e.g. `🔴 Open` → `✅ Resolved 2026-08-10 — <one-line note, link to commit/PR if applicable>`). Don't delete the row.
- New item: add a row to the relevant section (or add a new section if it doesn't fit the existing three). Keep the Owner column specific enough that someone reading it knows who to ping.
- If an item turns out to already be resolved elsewhere, still record it here rather than silently dropping it — same reasoning as `AI_PLAN_AND_CONTRACT_UPDATES.md`'s precedence rule: the record matters even after the fact.
