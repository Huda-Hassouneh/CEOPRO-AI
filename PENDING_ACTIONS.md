# CEOPRO AI — Pending Actions for Other Teams / Humans

Things discovered during AI/ML-track work that need a decision or action from **someone other than
the AI/ML track** — a different team, or a human with permissions this session doesn't have. Nothing
in this file gets fixed by AI/ML work; it's a visibility list so the right owner notices it.

**Convention:** unlike `AI_PROGRESS.md` (strictly append-only), rows here get their **Status**
updated in place once resolved — add a one-line resolution note rather than deleting the row, so
there's still a record of what happened. New items get added to the bottom of their section.

---

## Blocking (something can't be built until this lands)

| # | Item | Owner | Blocks | Status |
|---|---|---|---|---|
| 1 | Add `pgvector` extension + a chunk-storage table to schema | Infra/DB | Semantic (embedding/FAISS) retrieval for Phase 3 RAG Chatbot (spec §21) | ✅ Resolved 2026-08-08 — `docker-compose.yml`'s `postgres` image tag fixed to `pgvector/pgvector:pg15` (confirmed pulls). Fixed directly this round (explicit authorization to touch infra files, not the usual AI/ML-track boundary) rather than just flagged. |
| 2 | Add Row-Level Security policies for tenant isolation | Infra/DB | Defense-in-depth on multi-tenancy (spec §10, §37) | 🔴 Open, more serious than previously understood — the RLS migration's `ai_recommendations` bug (item #21) is fixed, but the isolation itself cannot work at all as currently deployed, for a reason deeper than the missing `FORCE ROW LEVEL SECURITY`: confirmed by spinning up `pgvector/pgvector:pg15` with the **exact** `POSTGRES_USER=ceopro_admin` `docker-compose.yml` configures — `SELECT rolsuper, rolbypassrls FROM pg_roles WHERE rolname = current_user;` returns `t, t`. The official Postgres image makes the bootstrap `POSTGRES_USER` a genuine superuser, and **superusers unconditionally bypass RLS regardless of `FORCE`** — this isn't a missing setting, it's a hard Postgres behavior with no override. Reproduced directly: applied `FORCE ROW LEVEL SECURITY`, seeded two tenants, queried as `ceopro_admin` with zero tenant context set — both tenants' rows came back anyway. See item #25 for what an actual fix requires (it's not just "add `FORCE`"). |
| 3 | Add `currency_rates` table | Infra/DB | Cross-currency pricing logic (spec §9, §19) | ✅ Resolved 2026-08-07 — table landed in `init_schema.sql`. Wired into `src/ai/pricing/currency.py` ([PR #5](https://github.com/Huda-Hassouneh/CEOPRO-AI/pull/5), merged). |
| 4 | `reviews`/`news_record`/`social_mention`/`extracted_entity`/`sentiment_results` tables don't exist | Infra/DB | NER persistence (spec §15) and sentiment analysis (spec §16) | 🟡 Partially resolved. `reviews` and `sentiment_results` landed in `init_schema.sql`, unblocking sentiment analysis entirely — `src/ai/sentiment/` is now built and tested (see `AI_PROGRESS.md`). `extracted_entity`/`news_record`/`social_mention` are now *defined* (migration `20260807230553_add_market_intelligence_tables.sql`), but — like the pgvector and RLS migrations — not actually deployable yet, since nothing applies the `migrations/` folder to the real schema; see item #22. NER persistence therefore remains blocked in practice despite the table definitions existing. |
| 5 | No real competitor price data or scraper running (`competitor_prices` table exists but is empty) | AI Market Scraper Service owner (per `DATA_OWNERSHIP_AND_CONTRACTS.md`) | Phase 6 Competitor Ranking (spec §20); Phase 5 Price Intelligence logic is now built and tested against seeded data (see `AI_PROGRESS.md`, `src/ai/pricing/`) but has nothing real to run against | 🟡 Open — AI/ML side is ready and waiting; the scraper itself is still the blocker |
| 18 | No real review data or review-collection service running (`reviews` table exists but is empty); also no owner is assigned for it in `DATA_OWNERSHIP_AND_CONTRACTS.md` at all (unlike `competitor_prices`, which names an "AI Market Scraper Service" owner — `reviews` has no equivalent entry) | Unassigned — needs a team/service to own it | Phase 4 sentiment analysis (spec §16); `src/ai/sentiment/` is built and tested against seeded data (see `AI_PROGRESS.md`) but has nothing real to run against | 🔴 Open, new 2026-08-07 |
| 19 | `src/infrastructure/database/seed_demo_data.py` (new, added 2026-08-07) does not run against the actual schema | Whoever owns `seed_demo_data.py` (Infra/DB) | Anyone running this script gets an immediate crash, not seeded demo data | ✅ Resolved 2026-08-08 — rewrote against the verified real schema (correct table/column names throughout, a stdlib Poisson sampler replacing the nonexistent `random.poissonvariate`, `conn`/`cursor` initialized before the `try` block, hardcoded credential fallback removed, deterministic UUIDs so re-running is safe for dimension data). Verified end-to-end against a real disposable Postgres: seeds 2 tenants/2 users/6 products/6 inventory rows/180 transactions/6 forecasts/3 currency rates/1 RAG chunk, and re-running doesn't duplicate the dimension tables (transaction/forecast counts legitimately grow on re-run, since they're time-series event data). |
| 20 | Two byte-identical migration files landed within the same session (market-intelligence tables) | Infra/DB | Would break any future migration runner (confirmed: second file fails with `relation "news_record" already exists` when applied after the first) | ✅ Resolved 2026-08-08 — deleted the duplicate (`20260807230710_add_market_intelligence_tables.sql`), kept the earlier-timestamped one. |
| 21 | `src/infrastructure/database/migrations/20260807230419_add_row_level_security.sql` references `ai_recommendations`, a table that doesn't exist anywhere in `init_schema.sql` | Infra/DB | Applying this migration errored out partway through | ✅ Resolved 2026-08-08 — traced the actual history (`git log -S"ai_recommendations"`): this table was renamed to `evidence_records` in commit `613ec53`, well before this migration was written against a stale table list. Fixed the reference; the full migration sequence (`init_schema.sql` + all 4 migration files, in filename order) now applies with zero errors, verified against a real disposable Postgres. |
| 22 | Four new `.sql` files landed 2026-08-07 in a new `src/infrastructure/database/migrations/` folder (campaigns table, RLS, and the two market-intelligence duplicates above) — **nothing in the repo applies them**: not `docker-compose.yml` (no volume mount for that folder, and no mount for `init_schema.sql` either — `docker-entrypoint-initdb.d` isn't wired up at all), not any CI workflow, not any shell/Python script anywhere (confirmed via a repo-wide search for references to `migrations/` or `init_schema.sql` in `.yml`/`.yaml`/`.sh`/`Dockerfile*` files — zero matches) | Infra/DB | Every one of items #1, #2, #4's "landed but not deployable" notes traces back to this — the schema keeps growing as files, but there's no mechanism turning a fresh `postgres:pg15`-family container into a container with any of `init_schema.sql`, RLS, or the new tables applied, short of someone running `psql -f` by hand (which is what this session's own testing has been doing) | 🔴 Open — the single highest-leverage fix available: one migration runner (or one `docker-entrypoint-initdb.d` mount ordering `init_schema.sql` + the four migration files) would resolve the "landed but not deployable" half of items #1, #2, and #4 in one change. Deliberately not built as part of 2026-08-08's fixes — a migration runner is new infrastructure, not a bug fix, and is a bigger design decision (which tool, how it integrates with CI/deploy) than this pass was scoped for. |
| 25 | RLS tenant isolation cannot work at all under the current deployment config — `FORCE ROW LEVEL SECURITY` alone is **not** sufficient, corrected from an earlier version of this row that understated the problem. `docker-compose.yml` configures `POSTGRES_USER=ceopro_admin`, and the official Postgres/pgvector image makes that bootstrap user a genuine superuser (`rolsuper=t`, `rolbypassrls=t`, confirmed via `pg_roles`) — **superusers unconditionally bypass RLS, `FORCE` has no effect on them, this is a hard Postgres behavior with no override**. Reproduced directly with the exact `docker-compose.yml` config: seeded two tenants, applied `FORCE ROW LEVEL SECURITY`, queried as `ceopro_admin` with zero tenant context set — both tenants' rows returned anyway. | Whoever owns the RLS/tenant-isolation architecture decision (Infra + AI/ML + any future backend) | This is the actual multi-tenancy safety net (spec §10, §37 call tenant isolation mandatory) and it currently provides **zero** protection no matter what policies exist, because of *which role* the app connects as, not because a policy is missing or misconfigured | 🔴 Open, new 2026-08-08 — a real fix needs: (a) a genuinely separate, non-superuser database role for the app to connect as day-to-day (reserving `ceopro_admin`/superuser only for migrations/bootstrap), granted table privileges but not `BYPASSRLS`; (b) `FORCE ROW LEVEL SECURITY` on every tenant-scoped table; (c) every DB-connecting code path (`src/ai/` today — confirmed zero uses of `SET app.current_tenant_id` anywhere — plus any future backend) retrofitted to set that session variable per connection. All three together, not any one alone. |
| 26 | ~~`ceopro_admin` cannot run `CREATE EXTENSION vector`~~ — **retracted 2026-08-08**, this row was based on a test setup that didn't match the real deployment: I'd manually created `ceopro_admin` as a plain non-superuser role to test item #25, and that role genuinely can't create extensions. Re-tested against the *actual* `docker-compose.yml` config (`POSTGRES_USER=ceopro_admin` via the official image's own bootstrap, no manual role creation) — `ceopro_admin` there is a superuser (see item #25) and `CREATE EXTENSION vector` succeeds without issue. Recorded here rather than silently deleted, per this file's own convention, since the earlier version of this row was live for a few hours and worth a visible correction. | — | — | ✅ Not an issue — retracted, see note |

## Needs a decision (not blocking yet, but ambiguous/contested)

| # | Item | Owner | Why it matters | Status |
|---|---|---|---|---|
| 6 | `src/infrastructure/messaging/ai_consumer.py` already consumes `market.intelligence.raw` and writes to `evidence_records` under infra's ownership — functionally overlaps with where a future AI market-intelligence module would live | Whoever owns both infra/messaging and the AI roadmap | Two systems risk double-writing `evidence_records` once that AI module is built | 🟠 Open — needs an explicit call, not resolvable by either side alone. Separately, see item #23: this file currently fails on every message it processes, regardless of this ownership question. |
| 7 | `model_versions` has no `artifact_path` column, so trained model binaries can't be pointed at their MinIO location (`ceopro-ai-artifacts`) yet | Backend/DB | Small, additive schema change (`ALTER TABLE model_versions ADD COLUMN artifact_path VARCHAR(500);`) — low risk, just needs someone to run it | 🟡 Open — small ask, easy to unblock |
| 8 | `.github/workflows/staging-deployment.yml` builds/deploys `Dockerfile.ai` and a `docker compose ... ai` service, and imports `src.ai.main`/`src.backend.main` for OpenAPI spec extraction — **none of `Dockerfile.ai`, `src/ai/main.py`, or `src/backend/main.py` exist anywhere in the repo, on any branch** | Infra/DevOps | CI will fail the first time this workflow actually runs end-to-end (currently only triggers on push to `main`) | 🟠 Open — re-confirmed 2026-08-07 after two more commits touched this file's CI logic; the underlying missing-files problem is unchanged |
| 9 | No root-level `requirements.txt`, but `Dockerfile.backend` runs `pip install -r requirements.txt` | Backend/Infra | Backend Docker build is currently broken | 🟠 Open — discovered during this session's repo audit |
| 14 | `products` table has no `cost`/COGS column | Backend/DB | Spec §19 calls for margin-based pricing guardrails; without a cost basis, `src/ai/pricing/guardrails.py` can only bound *how much a suggested price can move*, not *whether it stays profitable* — a materially weaker guardrail | 🟡 Open — small ask (`ALTER TABLE products ADD COLUMN cost NUMERIC(10, 2);`), same shape as item #7 |
| 15 | `src/ai/rag/data_access.py` only decodes MinIO objects as plain UTF-8 text — PDF/DOCX aren't extracted, though `MINIO_STORAGE_ARCHITECTURE.md` explicitly expects "supplier PDFs, business text files" in `ceopro-rag-knowledge` | AI/ML (self-flagged — not someone else's blocker, but noted here so it isn't silently assumed done) | Any uploaded PDF/DOCX in that bucket is currently invisible to RAG ingestion, not an error, just silently unmatched by any document row that references it | 🟡 Open — needs `pypdf`/`python-docx` added; not started |

## Security

| # | Item | Owner | Status |
|---|---|---|---|
| 10 | Hardcoded Postgres password fallback in `src/infrastructure/monitoring/watchdog.py` (`DATABASE_URL` default embedded a real-looking password in a public repo) | Whoever owns `watchdog.py` (Infra) | ✅ Resolved 2026-08-07 — `DATABASE_URL` now required (raises `RuntimeError` if unset), no hardcoded fallback. |
| 16 | `watchdog.py` still doesn't parse as valid Python — module docstring missing its opening/closing `"""` | Whoever owns `watchdog.py` (Infra) | ✅ Resolved 2026-08-08 — restored the docstring quoting; confirmed via `ast.parse()` and `py_compile` that it now imports cleanly. |
| 17 | `docker-compose.yml`'s `postgres` service image doesn't include pgvector | Infra/DevOps | ✅ Resolved 2026-08-08 — same fix as item #1 (`pgvector/pgvector:pg15`). |
| 23 | `src/infrastructure/messaging/ai_consumer.py` (item #6's file) had two separate real bugs: (a) `_resolve_db_url()` reintroduced the hardcoded-credential-fallback pattern already fixed once in `watchdog.py`; (b) `_write_ingestion_staging_record()` hardcoded `job_id = '00000000-0000-0000-0000-000000000000'::uuid`, violating the `fk_staging_job` FK on every single insert — confirmed the consumer had a 100% silent failure rate on every message it ever processed | Whoever owns `ai_consumer.py` (Infra) | ✅ Resolved 2026-08-08 — credential fallback removed (raises if `DATABASE_URL` unset, matching `watchdog.py`'s pattern); the consumer now inserts a real `ingestion_jobs` row per message and uses its actual `job_id`. Verified by reproducing the exact insert against a real disposable Postgres running the current schema — succeeds. |
| 24 | `docker-compose.yml`'s `grafana` service requires `${GRAFANA_ADMIN_USER}`/`${GRAFANA_ADMIN_PASSWORD}`, undocumented in `.env.example`; separately, `GRAFANA_ADMIN_PASSWORD`'s example value was a real-looking secret rather than this file's own `change_this_password` placeholder convention; separately, `ALERT_WEBHOOK_URL` (read in `watchdog.py`) was also undocumented | Infra/DevOps | ✅ Resolved 2026-08-08 — `GRAFANA_ADMIN_PASSWORD` now uses the placeholder convention consistent with every other secret in the file; `ALERT_WEBHOOK_URL` documented. |
| 29 | **Recurring pattern of unreviewed direct-to-`main` writes bypassing the PR workflow**, some introducing real regressions — investigated 2026-08-14 after `init_schema.sql` and `migrations/20260807230553_add_market_intelligence_tables.sql` both showed up corrupted/rewritten during a routine merge. Full commit-by-commit table below this one. In short: 26 commits (2026-08-03 → 2026-08-13, not counting normal PR merges) share an identical signature — commit message is exactly `Update <filename>` (GitHub's own auto-filled placeholder when you edit a file via github.com's browser file editor and don't type a custom message, or via the equivalent Contents API call), author is always `Huda-Hassouneh <huda.hasouneh@gmail.com>`, committer is always `GitHub <noreply@github.com>`. Most of these are legitimate incremental edits (adding tables, removing hardcoded credentials, port fixes). Three are not: (a) `cd3ddb3` (2026-08-12 00:50) replaced the entire working 21-table `init_schema.sql` with an unreviewed, breaking 26-table redesign (JSONB multilingual fields, `users.tenant_id` removed) — see item #30 below; (b) `e07e4e4` (2026-08-08 02:25) replaced `migrations/20260807230553_...sql`'s real `CREATE TABLE` SQL with unrelated non-functional Python code — this is the same single incident already logged as item #27 on the (unmerged) `claude/rls-app-role-and-migration-fix` branch, fixed there but never corrected on `main` itself, opened as its own standalone fix: [PR](https://github.com/Huda-Hassouneh/CEOPRO-AI/pull/new/fix-market-intelligence-migration); (c) `cdac377`+`492d9b6` (`watchdog.py`, 2026-08-07) and `444cf2e`+`ad725d9` (`security.py`, 2026-08-08) both show **literal leftover PowerShell here-string/file-write syntax committed as if it were the target file's Python content** (`@'` ... `'@ | Out-File -FilePath ... -Encoding utf8` for watchdog.py; `$content = @' ... '@` + `[System.IO.File]::WriteAllText($targetPath, $content, ...)` for security.py) — strong, concrete evidence of a local PowerShell-based script/automation tool whose own un-executed source text ended up as the committed file content, not a human typing in a browser. `security.py` self-corrected 3 minutes later (`ad725d9`) and is valid Python on `main` today; `watchdog.py` stayed broken (item #16) until fixed manually this session. | Whoever owns write access / any integration with push rights to `main` (Infra/DevOps + repo admin) | 🔴 Open — needs: (1) branch protection on `main` requiring PRs (see item #31's findings), (2) identifying what has direct write access today (GitHub web UI session vs. a PAT/OAuth App/Action — not resolvable from git metadata alone, needs the GitHub organization/repo Audit Log, which this session has no access to) |
| 30 | `init_schema.sql`'s unreviewed 26-table redesign (see item #29, commit `cd3ddb3` and its two follow-ups `f6a49e2`/`0f35506`) is incompatible with the 21-table schema essentially all of `src/ai/`'s existing code was built and tested against (e.g. `products.product_name` as plain text vs. new `JSONB`; `users.tenant_id` direct FK vs. new `tenant_users` pivot table) | Whoever owns the schema architecture decision — needs explicit reconciliation, not a silent merge either direction | 🔴 Open — comparison table requested, not yet reconciled; do not merge either version into the other without an explicit decision |

### Item #29 detail — full commit-by-commit history

All 26 commits sharing the `Update <filename>` / author=Huda-Hassouneh / committer=GitHub signature, on `main`, 2026-08-03 → 2026-08-13 (PR-merge commits excluded — those are normal). "Verdict" is based only on content this session has actually diffed and either previously investigated or checked while compiling this table — entries marked *not audited* carry no claim either way, just a flag for future review.

| # | Time (+03) | Commit | File | Verdict | Current status on `main` |
|---|---|---|---|---|---|
| 1 | 08-03 00:39 | `1185b9b` | `docker-compose.yml` | ✅ Correct — replaced hardcoded Postgres/MinIO passwords with `${...}` env var references | Stands, still correct |
| 2 | 08-06 21:15 | `a93dcf1` | `src/infrastructure/init_broker.py` | ⚪ Not audited this session | Unreviewed |
| 3 | 08-07 07:55 | `041d7e0` | `init_schema.sql` | ✅ Correct — added `evidence_records`, `recommendation_outcomes`, `data_sources`, `ingestion_jobs`, `model_versions`, `audit_logs` tables (all part of the schema `src/ai/` was later built against) | Stands, still correct |
| 4 | 08-07 07:57 | `a5030ad` | `.env.example` | ⚪ Not audited this session | Unreviewed |
| 5 | 08-07 07:58 | `78b216d` | `docker-compose.yml` | ✅ Correct — port mapping normalization (`5435:5432` → `5432:5432`) | Stands, still correct |
| 6 | 08-07 07:59 | `6efdc9c` | `src/infrastructure/security.py` | ✅ Correct — added required-`JWT_SECRET`-at-startup check, no hardcoded fallback | Superseded by #17 below |
| 7 | 08-07 07:59 | `5cbe877` | `src/infrastructure/check_connectivity.py` | ⚪ Not audited this session | Unreviewed |
| 8 | 08-07 08:00 | `010d97d` | `DATA_OWNERSHIP_AND_CONTRACTS.md` | ⚪ Not audited this session (docs-only, low risk) | Unreviewed |
| 9 | 08-07 11:51 | `a62e895` | `src/infrastructure/messaging/token_rotator.py` | ⚪ Not audited this session | Unreviewed |
| 10 | 08-07 11:56 | `8ae0434` | `src/infrastructure/messaging/ai_consumer.py` | 🟡 Partial check — this specific edit added the correct `DATABASE_URL`-required pattern; the credential-fallback regression item #23 (elsewhere in this file) later found in this same file was **not** introduced here, so it must trace to a later, unaudited change | The #23 bugs were found and fixed 2026-08-08 (this session) |
| 11 | 08-07 11:57 | `e2fcc3a` | `src/infrastructure/messaging/producer.py` | ⚪ Not audited this session | Unreviewed |
| 12 | 08-07 12:36 | `07891bb` | `src/infrastructure/database/backup_verifier.py` | ⚪ Not audited this session | Unreviewed |
| 13 | 08-07 12:41 | `03956e9` | `src/infrastructure/messaging/app_consumer.py` | ⚪ Not audited this session | Unreviewed |
| 14 | 08-07 12:43 | `5ec39c6` | `src/infrastructure/Dockerfile.backend` | ⚪ Not audited this session | Unreviewed |
| 15 | 08-07 13:19 | `cdac377` | `src/infrastructure/monitoring/watchdog.py` | 🔴 **Bug introduced** — replaced the module docstring's opening `"""` with a literal PowerShell here-string opener `@'`; the file's own trailing `'@ \| Out-File -FilePath ... -Encoding utf8` (added by this same commit) is unmistakably leftover PowerShell script syntax, not Python — strong evidence of a local PowerShell-based write script whose own source leaked into the committed file instead of being executed | Item #16 — fixed manually this session (2026-08-08), restored proper `"""` quoting |
| 16 | 08-07 17:42 | `b8cbed9` | `init_schema.sql` | ✅ Correct — added `pgvector` extension, `product_price_history`, `reviews`, `sentiment_results`, `rag_document_chunks`, `currency_rates`, `import_staging_rows` tables | Stands, still correct |
| 17 | 08-07 18:57 | `22d90b4` | `init_schema.sql` | ⚪ Empty commit — zero content difference from its parent (`b8cbed9`); GitHub's web editor produces exactly this when a file is opened and re-saved with no real edit | No-op, nothing to fix |
| 18 | 08-07 20:00 | `492d9b6` | `src/infrastructure/monitoring/watchdog.py` | 🟡 Partial fix — removed the PowerShell `@'` opener and the trailing `'@ \| Out-File ...` line from #15's damage, but never restored the actual `"""` triple-quotes, so the file remained invalid Python | Fixed alongside item #16, same session |
| 19 | 08-07 23:16 | `55ae05e` | `PENDING_ACTIONS.md` | ⚪ Not audited this session (self-edit of this tracking file) | Unreviewed |
| 20 | 08-08 02:18 | `444cf2e` | `src/infrastructure/security.py` | 🔴 **Bug introduced** — file began with the literal line `$content = @'`, i.e. a PowerShell variable assignment opening a here-string, followed by what should have been the file's real Python content | Fixed 3 minutes later by #22 |
| 21 | 08-08 02:20 | `b042661` | `.github/workflows/staging-deployment.yml` | ⚪ Not audited in detail — large diff (230 lines removed), previously noted in `AI_PROGRESS.md` as fixing hardcoded secrets while leaving the missing-files problem (item #8) unchanged | Partially reviewed in an earlier session pass |
| 22 | 08-08 02:21 | `ad725d9` | `src/infrastructure/security.py` | ✅ Self-corrected — removed the leftover `$content = @'` opener and the trailing PowerShell `[System.IO.File]::WriteAllText(...)` / `Write-Host` block #20 left behind. Confirmed via `py_compile` this session: the file is valid Python on `main` today | Fixed, stands correct (one code smell noted in passing, not confirmed as a bug: a bare `except Exception: pass` around token-rotation validation) |
| 23 | 08-08 02:25 | `e07e4e4` | `migrations/20260807230553_add_market_intelligence_tables.sql` | 🔴 **Bug introduced** — replaced the real `CREATE TABLE news_record/social_mention/extracted_entity` SQL with an unrelated, non-functional Python `get_tenant_connection()` helper | Still broken on `main` today — standalone fix opened, see item #29 above |
| 24 | 08-12 00:50 | `cd3ddb3` | `init_schema.sql` | 🔴 **Breaking, unreviewed redesign** — replaced the entire 21-table schema with a new 26-table one (JSONB multilingual fields, `users.tenant_id` removed, new `tenant_users`/`system_roles` RBAC tables) | Still on `main` today — see item #30, comparison requested, not reconciled |
| 25 | 08-12 01:17 | `f6a49e2` | `init_schema.sql` | Continuation of #24's redesign (formatting cleanup + one constraint tightened) — same open question as #24 | Still on `main` today |
| 26 | 08-13 13:40 | `0f35506` | `init_schema.sql` | Continuation of #24's redesign (added `system_roles`, `system_alerts`, `audit_logs`, expanded several other tables) — same open question as #24 | Still on `main` today |

**What git metadata cannot answer**: whether these commits were made by the account holder directly through github.com's browser editor, or by an automated script/integration using that account's credentials or an API token. Both produce identical author/committer/GPG signatures. The PowerShell-artifact commits (#15, #20) are the strongest evidence pointing toward local script automation specifically (a human pasting into a browser text box is unlikely to include `[System.IO.File]::WriteAllText(...)` boilerplate), but this is inference, not proof. The only source that could resolve this definitively is GitHub's organization/repository **Audit Log** (Settings → Audit log), which records actual originating session type (web / OAuth App / PAT name) and IP — this session has no access to it.

## Waiting on documents / approvals (not code)

| # | Item | Owner | Status |
|---|---|---|---|
| 11 | `AI_ENGINEERING_PLAN.md`, `AI_CONTRACT_CHANGES_AND_CLARIFICATIONS.md`, and the original `AI_PROGRESS.md` referenced by `AI_PLAN_AND_CONTRACT_UPDATES.md` (module-level detail, "Module E/J/K" references) don't exist anywhere in this repo's git history | Whoever authored `AI_PLAN_AND_CONTRACT_UPDATES.md` | 🟡 Open — acknowledged as "will provide later"; AI/ML work in the meantime is going directly off `MASTER_SPEC_v4.md` |
| 12 | `MASTER_SPEC_v4.md` and `AI_PLAN_AND_CONTRACT_UPDATES.md` are staged locally / pushed to `noorhassouneh-patch-1` but not yet merged to `main` (reported as pending approval, pusher lacks `main` permissions) | Whoever holds merge rights on `main` | 🟡 Open — not blocking AI/ML work (content already available on the patch branch), but other teams won't see these docs until merged |
| 13 | Orange infrastructure environment (CPU/GPU availability, on-prem vs. cloud) not yet confirmed (spec §28) | Whoever owns the Orange deployment relationship | ⚪ Deferred — confirmed CPU-only / no GPU "for now" for this track; revisit if/when GPU becomes available |

---

## How to update this file

- Resolving an item: change its Status cell (e.g. `🔴 Open` → `✅ Resolved 2026-08-10 — <one-line note, link to commit/PR if applicable>`). Don't delete the row.
- New item: add a row to the relevant section (or add a new section if it doesn't fit the existing three). Keep the Owner column specific enough that someone reading it knows who to ping.
- If an item turns out to already be resolved elsewhere, still record it here rather than silently dropping it — same reasoning as `AI_PLAN_AND_CONTRACT_UPDATES.md`'s precedence rule: the record matters even after the fact.
