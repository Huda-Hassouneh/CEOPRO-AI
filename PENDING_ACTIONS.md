# CEOPRO AI — Pending Actions for Other Teams / Humans

Things discovered during AI/ML-track work that need a decision or action from **someone other than
the AI/ML track** — a different team, or a human with permissions this session doesn't have. Nothing
in this file gets fixed by AI/ML work; it's a visibility list so the right owner notices it.

**Convention:** unlike `AI_PROGRESS.md` (strictly append-only), rows here get their **Status**
updated in place once resolved — add a one-line resolution note rather than deleting the row, so
there's still a record of what happened. New items get added to the bottom of their section.

---

## Blocking (something can't be built until this lands)

| # | Item | Owner | Blocks | Status |
|---|---|---|---|---|
| 1 | Add `pgvector` extension + a chunk-storage table to schema | Infra/DB | Semantic (embedding/FAISS) retrieval for Phase 3 RAG Chatbot (spec §21) | ✅ Resolved 2026-08-08 — `docker-compose.yml`'s `postgres` image tag fixed to `pgvector/pgvector:pg15` (confirmed pulls). Fixed directly this round (explicit authorization to touch infra files, not the usual AI/ML-track boundary) rather than just flagged. Fully wired up 2026-08-31: `rag/data_access.py`/`pipeline.py` now actually read and write `rag_document_chunks.embedding` (the column existed, unused, since 2026-08-27's `20260827000100_add_rag_embedding_column.sql`) — see item #45. |
| 2 | Add Row-Level Security policies for tenant isolation | Infra/DB | Defense-in-depth on multi-tenancy (spec §10, §37) | 🔴 Open, more serious than previously understood — the RLS migration's `ai_recommendations` bug (item #21) is fixed, but the isolation itself cannot work at all as currently deployed, for a reason deeper than the missing `FORCE ROW LEVEL SECURITY`: confirmed by spinning up `pgvector/pgvector:pg15` with the **exact** `POSTGRES_USER=ceopro_admin` `docker-compose.yml` configures — `SELECT rolsuper, rolbypassrls FROM pg_roles WHERE rolname = current_user;` returns `t, t`. The official Postgres image makes the bootstrap `POSTGRES_USER` a genuine superuser, and **superusers unconditionally bypass RLS regardless of `FORCE`** — this isn't a missing setting, it's a hard Postgres behavior with no override. Reproduced directly: applied `FORCE ROW LEVEL SECURITY`, seeded two tenants, queried as `ceopro_admin` with zero tenant context set — both tenants' rows came back anyway. See item #25 for what an actual fix requires (it's not just "add `FORCE`"), and item #32 for a second, independent RLS bug found later (2026-08-27 deep edge-case pass) once a real non-superuser role was actually exercised end-to-end. **Still open even after #32's fix**: nothing in `src/ai/` calls `SET`/`set_config` for `app.current_tenant_id`/`app.current_user_id` anywhere — confirmed again during the 2026-08-27 PR-triage pass. `ceopro_app` (the non-superuser role) exists and its policies now work correctly (#32), but nothing connects as it or sets its session context yet, so in practice every query today still runs as the superuser `ceopro_admin` with zero isolation. **Useful starting point found, not merged**: PR #15 has an unmerged `src/ai/db.py` with a `set_tenant_context(conn, tenant_id)` helper (`SET app.current_tenant_id = %s`) plus a call site in `forecasting/consumer.py` — a real, reasoned design (session-level `SET`, not `SET LOCAL`, because these connections are long-lived and not pooled; called once per unit of work, not once at connect time, since one connection serves many tenants over its life). Two things need fixing before it's usable against `Final_schema.sql`, not just copied over: (a) it only sets `app.current_tenant_id` — `get_current_tenant()` requires `app.current_user_id` too (see item #32), so wiring this in as-is would make every RLS-scoped query return zero rows, not the correct tenant's rows; (b) it was written and "verified" against the old `init_schema.sql`'s simpler RLS (no `get_current_tenant()`, no `tenant_users` membership check), never run against `Final_schema.sql` at all. |
| 3 | Add `currency_rates` table | Infra/DB | Cross-currency pricing logic (spec §9, §19) | ✅ Resolved 2026-08-07 — table landed in `init_schema.sql`. Wired into `src/ai/pricing/currency.py` ([PR #5](https://github.com/Huda-Hassouneh/CEOPRO-AI/pull/5), merged). |
| 4 | `reviews`/`news_record`/`social_mention`/`extracted_entity`/`sentiment_results` tables don't exist | Infra/DB | NER persistence (spec §15) and sentiment analysis (spec §16) | 🟡 Partially resolved. `reviews` and `sentiment_results` landed in `init_schema.sql`, unblocking sentiment analysis entirely — `src/ai/sentiment/` is now built and tested (see `AI_PROGRESS.md`). `extracted_entity`/`news_record`/`social_mention` are now *defined* (migration `20260807230553_add_market_intelligence_tables.sql`), but — like the pgvector and RLS migrations — not actually deployable yet, since nothing applies the `migrations/` folder to the real schema; see item #22. NER persistence therefore remains blocked in practice despite the table definitions existing. |
| 5 | No approved real competitor source/mappings are configured yet (`competitor_prices` remains empty until that human/compliance setup is completed) | Product/compliance owner must approve sources and create tenant mappings; AI Market Scraper Service is implemented in `src/market_scraper/` | Phase 6 Competitor Ranking (spec §20); Phase 5 Price Intelligence has no real observations until an approved source is configured |  Partially resolved 2026-08-29, collector coverage completed 2026-08-31 — policy engine, Redis worker allocation, mapped crawling, job tracking, and canonical `competitor_prices` persistence are built/tested; `google_places` and `amazon_paapi` official-API collectors added 2026-08-31, closing the last of the three originally-requested integrations (Google Places, Amazon PA-API, generic website — the last already covered by the pre-existing `standards` collector). Remaining external action unchanged: approve real sources, configure `data_sources` + `competitor_product_mappings`, and (new, for the two API-key-gated collectors) populate `data_sources.connection_credentials_vault` — see item #42. |
| 18 | No real review data or review-collection service running (`reviews` table exists but is empty); also no owner is assigned for it in `DATA_OWNERSHIP_AND_CONTRACTS.md` at all (unlike `competitor_prices`, which names an "AI Market Scraper Service" owner — `reviews` has no equivalent entry) | Unassigned — needs a team/service to own it | Phase 4 sentiment analysis (spec §16); `src/ai/sentiment/` is built and tested against seeded data (see `AI_PROGRESS.md`) but has nothing real to run against | 🔴 Open, new 2026-08-07 |
| 19 | `src/infrastructure/database/seed_demo_data.py` (new, added 2026-08-07) does not run against the actual schema | Whoever owns `seed_demo_data.py` (Infra/DB) | Anyone running this script gets an immediate crash, not seeded demo data | ✅ Resolved 2026-08-08 — rewrote against the verified real schema (correct table/column names throughout, a stdlib Poisson sampler replacing the nonexistent `random.poissonvariate`, `conn`/`cursor` initialized before the `try` block, hardcoded credential fallback removed, deterministic UUIDs so re-running is safe for dimension data). Verified end-to-end against a real disposable Postgres: seeds 2 tenants/2 users/6 products/6 inventory rows/180 transactions/6 forecasts/3 currency rates/1 RAG chunk, and re-running doesn't duplicate the dimension tables (transaction/forecast counts legitimately grow on re-run, since they're time-series event data). |
| 20 | Two byte-identical migration files landed within the same session (market-intelligence tables) | Infra/DB | Would break any future migration runner (confirmed: second file fails with `relation "news_record" already exists` when applied after the first) | ✅ Resolved 2026-08-08 — deleted the duplicate (`20260807230710_add_market_intelligence_tables.sql`), kept the earlier-timestamped one. |
| 21 | `src/infrastructure/database/migrations/20260807230419_add_row_level_security.sql` references `ai_recommendations`, a table that doesn't exist anywhere in `init_schema.sql` | Infra/DB | Applying this migration errored out partway through | ✅ Resolved 2026-08-08 — traced the actual history (`git log -S"ai_recommendations"`): this table was renamed to `evidence_records` in commit `613ec53`, well before this migration was written against a stale table list. Fixed the reference; the full migration sequence (`init_schema.sql` + all 4 migration files, in filename order) now applies with zero errors, verified against a real disposable Postgres. |
| 22 | Four new `.sql` files landed 2026-08-07 in a new `src/infrastructure/database/migrations/` folder (campaigns table, RLS, and the two market-intelligence duplicates above) — **nothing in the repo applies them**: not `docker-compose.yml` (no volume mount for that folder, and no mount for `init_schema.sql` either — `docker-entrypoint-initdb.d` isn't wired up at all), not any CI workflow, not any shell/Python script anywhere (confirmed via a repo-wide search for references to `migrations/` or `init_schema.sql` in `.yml`/`.yaml`/`.sh`/`Dockerfile*` files — zero matches) | Infra/DB | Every one of items #1, #2, #4's "landed but not deployable" notes traces back to this — the schema keeps growing as files, but there's no mechanism turning a fresh `postgres:pg15`-family container into a container with any of `init_schema.sql`, RLS, or the new tables applied, short of someone running `psql -f` by hand (which is what this session's own testing has been doing) | 🔴 Open — the single highest-leverage fix available: one migration runner (or one `docker-entrypoint-initdb.d` mount ordering `init_schema.sql` + the four migration files) would resolve the "landed but not deployable" half of items #1, #2, and #4 in one change. Deliberately not built as part of 2026-08-08's fixes — a migration runner is new infrastructure, not a bug fix, and is a bigger design decision (which tool, how it integrates with CI/deploy) than this pass was scoped for. |
| 25 | RLS tenant isolation cannot work at all under the current deployment config — `FORCE ROW LEVEL SECURITY` alone is **not** sufficient, corrected from an earlier version of this row that understated the problem. `docker-compose.yml` configures `POSTGRES_USER=ceopro_admin`, and the official Postgres/pgvector image makes that bootstrap user a genuine superuser (`rolsuper=t`, `rolbypassrls=t`, confirmed via `pg_roles`) — **superusers unconditionally bypass RLS, `FORCE` has no effect on them, this is a hard Postgres behavior with no override**. Reproduced directly with the exact `docker-compose.yml` config: seeded two tenants, applied `FORCE ROW LEVEL SECURITY`, queried as `ceopro_admin` with zero tenant context set — both tenants' rows returned anyway. | Whoever owns the RLS/tenant-isolation architecture decision (Infra + AI/ML + any future backend) | This is the actual multi-tenancy safety net (spec §10, §37 call tenant isolation mandatory) and it currently provides **zero** protection no matter what policies exist, because of *which role* the app connects as, not because a policy is missing or misconfigured | 🔴 Open, new 2026-08-08 — a real fix needs: (a) a genuinely separate, non-superuser database role for the app to connect as day-to-day (reserving `ceopro_admin`/superuser only for migrations/bootstrap), granted table privileges but not `BYPASSRLS`; (b) `FORCE ROW LEVEL SECURITY` on every tenant-scoped table; (c) every DB-connecting code path (`src/ai/` today — confirmed zero uses of `SET app.current_tenant_id` anywhere — plus any future backend) retrofitted to set that session variable per connection. All three together, not any one alone. |
| 26 | ~~`ceopro_admin` cannot run `CREATE EXTENSION vector`~~ — **retracted 2026-08-08**, this row was based on a test setup that didn't match the real deployment: I'd manually created `ceopro_admin` as a plain non-superuser role to test item #25, and that role genuinely can't create extensions. Re-tested against the *actual* `docker-compose.yml` config (`POSTGRES_USER=ceopro_admin` via the official image's own bootstrap, no manual role creation) — `ceopro_admin` there is a superuser (see item #25) and `CREATE EXTENSION vector` succeeds without issue. Recorded here rather than silently deleted, per this file's own convention, since the earlier version of this row was live for a few hours and worth a visible correction. | — | — | ✅ Not an issue — retracted, see note |

## Needs a decision (not blocking yet, but ambiguous/contested)

| # | Item | Owner | Why it matters | Status |
|---|---|---|---|---|
| 6 | `src/infrastructure/messaging/ai_consumer.py` already consumes `market.intelligence.raw` and writes to `evidence_records` under infra's ownership — functionally overlaps with where a future AI market-intelligence module would live | Whoever owns both infra/messaging and the AI roadmap | Two systems risk double-writing `evidence_records` once that AI module is built | 🟠 Open — needs an explicit call, not resolvable by either side alone. Separately, see item #23: this file currently fails on every message it processes, regardless of this ownership question. |
| 7 | `model_versions` has no `artifact_path` column, so trained model binaries can't be pointed at their MinIO location (`ceopro-ai-artifacts`) yet | Backend/DB | Small, additive schema change (`ALTER TABLE model_versions ADD COLUMN artifact_path VARCHAR(500);`) — low risk, just needs someone to run it | 🟡 Open — small ask, easy to unblock |
| 8 | `.github/workflows/staging-deployment.yml` builds/deploys `Dockerfile.ai` and a `docker compose ... ai` service, and imports `src.ai.main`/`src.backend.main` for OpenAPI spec extraction — **none of `Dockerfile.ai`, `src/ai/main.py`, or `src/backend/main.py` exist anywhere in the repo, on any branch** | Infra/DevOps | CI will fail the first time this workflow actually runs end-to-end (currently only triggers on push to `main`) | 🟠 Open — re-confirmed 2026-08-07 after two more commits touched this file's CI logic; the underlying missing-files problem is unchanged |
| 9 | No root-level `requirements.txt`, but `Dockerfile.backend` runs `pip install -r requirements.txt` | Backend/Infra | Backend Docker build is currently broken | 🟠 Open — discovered during this session's repo audit |
| 14 | ~~`products` table has no `cost`/COGS column~~ — **stale, corrected 2026-08-27**. This row went unreviewed while `Final_schema.sql` was adopted as canonical; `Final_schema.sql` already has `products.cost_price NUMERIC(12,4)` built in (confirmed directly: `grep cost_price src/infrastructure/database/Final_schema.sql`), and `pricing/guardrails.py::apply_margin_guardrail()` (built during the schema-fork rework) already uses it. | — | ✅ Not an issue — retracted, see note. Found and corrected during a full PR-triage pass, not from new work; recorded rather than silently deleted per this file's own convention. |
| 15 | `src/ai/rag/data_access.py` only decodes MinIO objects as plain UTF-8 text — PDF/DOCX aren't extracted, though `MINIO_STORAGE_ARCHITECTURE.md` explicitly expects "supplier PDFs, business text files" in `ceopro-rag-knowledge` | AI/ML (self-flagged — not someone else's blocker, but noted here so it isn't silently assumed done) | Any uploaded PDF/DOCX in that bucket is currently invisible to RAG ingestion, not an error, just silently unmatched by any document row that references it | 🟡 Still genuinely open (confirmed 2026-08-27 — `rag/data_access.py`'s own docstring still says "not added yet") — needs `pypdf`/`python-docx` added. **Note:** PR #15 (`claude/ai-engineering-plan-and-schema-alignment`, never merged) has ready-made code for exactly this — `pypdf`/`python-docx` added to requirements, `fetch_document_text()` routing by the MinIO object key's extension — worth reviewing as a starting point rather than building from scratch, though it predates this session's RLS/schema work and hasn't been checked against `Final_schema.sql`. |

## Security

| # | Item | Owner | Status |
|---|---|---|---|
| 10 | Hardcoded Postgres password fallback in `src/infrastructure/monitoring/watchdog.py` (`DATABASE_URL` default embedded a real-looking password in a public repo) | Whoever owns `watchdog.py` (Infra) | ✅ Resolved 2026-08-07 — `DATABASE_URL` now required (raises `RuntimeError` if unset), no hardcoded fallback. |
| 16 | `watchdog.py` still doesn't parse as valid Python — module docstring missing its opening/closing `"""` | Whoever owns `watchdog.py` (Infra) | ✅ Resolved 2026-08-08 — restored the docstring quoting; confirmed via `ast.parse()` and `py_compile` that it now imports cleanly. |
| 17 | `docker-compose.yml`'s `postgres` service image doesn't include pgvector | Infra/DevOps | ✅ Resolved 2026-08-08 — same fix as item #1 (`pgvector/pgvector:pg15`). |
| 23 | `src/infrastructure/messaging/ai_consumer.py` (item #6's file) had two separate real bugs: (a) `_resolve_db_url()` reintroduced the hardcoded-credential-fallback pattern already fixed once in `watchdog.py`; (b) `_write_ingestion_staging_record()` hardcoded `job_id = '00000000-0000-0000-0000-000000000000'::uuid`, violating the `fk_staging_job` FK on every single insert — confirmed the consumer had a 100% silent failure rate on every message it ever processed | Whoever owns `ai_consumer.py` (Infra) | ✅ Resolved 2026-08-08 — credential fallback removed (raises if `DATABASE_URL` unset, matching `watchdog.py`'s pattern); the consumer now inserts a real `ingestion_jobs` row per message and uses its actual `job_id`. Verified by reproducing the exact insert against a real disposable Postgres running the current schema — succeeds. |
| 24 | `docker-compose.yml`'s `grafana` service requires `${GRAFANA_ADMIN_USER}`/`${GRAFANA_ADMIN_PASSWORD}`, undocumented in `.env.example`; separately, `GRAFANA_ADMIN_PASSWORD`'s example value was a real-looking secret rather than this file's own `change_this_password` placeholder convention; separately, `ALERT_WEBHOOK_URL` (read in `watchdog.py`) was also undocumented | Infra/DevOps | ✅ Resolved 2026-08-08 — `GRAFANA_ADMIN_PASSWORD` now uses the placeholder convention consistent with every other secret in the file; `ALERT_WEBHOOK_URL` documented. |
| 27 | **Schema fork resolved: `Final_schema.sql` (26 tables) adopted as canonical**, superseding the old `init_schema.sql`. Confirmed with the project owner directly (2026-08-27) rather than assumed. All AI/ML PRs built against the old schema (`init_schema.sql`, `products.product_name` as plain text, a standalone `competitors` table, `reviews.subject_type`/`source_status`) need reworking against the new one (`product_name` JSONB, `global_competitors`/`tenant_competitors`/`competitor_product_mappings`, `reviews` product-only with no source-status column at all anymore) | AI/ML | ✅ Resolved 2026-08-27 — **verified against a real, disposable Postgres/Redis/MinIO stack** (Docker Desktop became available mid-session; root-caused and fixed a zombie-process/broken-pipe issue blocking it, see below), not just reviewed. `scripts/apply_migrations.py` ran clean against `Final_schema.sql` + all 11 migrations from an empty database, zero errors, twice (including idempotency/password-sync re-run). Full live-DB integration suite (`pytest -k integration_db`) for the four reworked modules — `sentiment/`, `mpi/`, `pricing/`, `extraction/` — is **fully green**: 0 failures, 0 errors. This live run caught 3 real bugs manual review and `sqlparse` had missed (see item #28's addendum and item #30), confirming this step wasn't redundant with the earlier code-complete pass. `forecasting/`'s integration tests still fail/error, but that's a separate, pre-existing, out-of-scope gap — see item #31. |
| 28 | `evidence_records` in `Final_schema.sql` is forecast-only (`forecast_id UUID NOT NULL`, only `metric_name`/`metric_value_json`/`contribution_weight` columns) — structurally cannot be written to by `pricing/`, `sentiment/`, `mpi/`, or `extraction/`, breaking spec §22's explicit "one consistent evidence architecture, shared by all AI outputs" requirement | Whoever owns the schema architecture decision (flagging, not asking permission — see resolution) | ✅ Resolved 2026-08-27 — `migrations/20260827000000_restore_shared_evidence_architecture.sql`: `forecast_id` made nullable, general-purpose columns (`category`, `source_module`, `explanation_text`, `confidence_score`, `source_record_ids`, `model_version`, `country_context`, `generated_at`) added back alongside the forecast-specific ones, which are untouched. A `chk_evidence_shape` constraint requires either `forecast_id` or the general-purpose columns to be populated — no half-empty rows. Chose extending the existing table over forking a second one, per `AI_PLAN_AND_CONTRACT_UPDATES.md`'s own precedent ("a second table would fork the evidence trail rather than extend it"). **Addendum, found by the live-DB run (item #27):** the first pass only dropped `NOT NULL` on `forecast_id` — `metric_name`, `metric_value_json`, and `contribution_weight` were still `NOT NULL` in `Final_schema.sql`'s original definition and were missed, so every non-forecasting evidence insert (`sentiment/`, `pricing/`, `mpi/`) still failed with `NotNullViolation` until an actual insert was attempted against a real table. Fixed in the same migration file; manual review and `sqlparse` had no way to catch this since neither executes the SQL. |
| 29 | Three concrete bugs found in code landed directly on `main` alongside the schema fork, none caught before landing since nothing was ever run against a live database: (a) `extraction/data_access.py::load_known_competitor_names()` queried `FROM competitors`, a table `Final_schema.sql` (landed in the same commit burst) doesn't have; (b) `pdf_adapter.py`/`xlsx_adapter.py` import `pdfplumber`/`openpyxl`, neither in `src/ai/requirements.txt`; (c) `docker-compose.yml`'s new `migrate` service runs `python scripts/apply_migrations.py`, which didn't exist anywhere in the repo | AI/ML (self-found, self-fixed) | ✅ Resolved 2026-08-27 — (a) rewritten to join `tenant_competitors`/`global_competitors`, also fixed to handle `product_name` now being JSONB (returns every language variant as a separate candidate name, not the raw JSON blob); (b) both added to `requirements.txt`; (c) `scripts/apply_migrations.py` written — a real migration runner (applies `Final_schema.sql` once, then `migrations/` in order, tracked in `schema_migrations`), plus 2 more real gaps found while building it and fixed in the same pass: `rag_document_chunks` had no `embedding` column at all (no `vector` extension created anywhere in `Final_schema.sql` either) — added via migration, sized at 384 dimensions (confirmed empirically against the actual production embedding model, not assumed); and no non-superuser app role existed for RLS to actually restrict anything against (same class of bug as item #25 on the old schema) — added via migration, password set out-of-band from `APP_DB_PASSWORD`, documented in `.env.example`. Also retired `migrations/20260807230419_add_row_level_security.sql` (referenced `transactions`/`competitors`, neither exists in `Final_schema.sql` — would hard-fail the whole migration sequence) and fixed `migrations/20260807225947_add_campaigns_table.sql` (used `uuid_generate_v4()`, needs an extension `Final_schema.sql` never creates). Two pre-existing test failures also found and fixed while verifying: `extract_discount()` dropped the `%` suffix from `normalized_value`, and `test_extractor.py` was never updated when `extract_entities()`'s signature changed (still called it with the old `known_product_names`/`known_competitor_names` kwargs) — both fixed. |
| 30 | Live-DB run (item #27) found two more test-fixture bugs, distinct from the schema/migration bugs in items #28/#29 — the fixtures were wrong, not the product code: (a) `test_sentiment_integration_db.py::_insert_review()` and `test_mpi_integration_db.py::_insert_scored_review()` never set `reviews.source_platform`, which is (and always was, independent of this rework) `NOT NULL` in `Final_schema.sql`; (b) `test_pricing_integration_db.py::_insert_competitor_price()` inserted a brand-new `global_competitors` row on every call, so any test seeding more than one price for the same competitor name (e.g. 3 observed prices from "Rival Pharmacy") hit `uq_competitor_private` and failed | AI/ML (self-found, self-fixed) | ✅ Resolved 2026-08-27 — (a) both fixtures now pass `source_platform='GOOGLE'`; (b) the fixture now looks up an existing `global_competitors`/`competitor_product_mappings` row for that `(tenant_id, competitor_name)` / `(tenant_id, competitor_id, product_id)` pair before inserting a new one, matching how the real ingestion code would behave on a repeat observation. |
| 32 | **`get_current_tenant()` (the function every RLS policy in `Final_schema.sql` calls) recurses infinitely and crashes the instant it's actually evaluated by a non-superuser role with real tenant context set** — found during a 2026-08-27 deep edge-case testing pass, specifically *because* that pass connected as the real `ceopro_app` role instead of `ceopro_admin` (every prior verification this session, including item #27's, connected as the superuser, which bypasses RLS — and therefore never evaluates any policy at all, including the broken one). The function is `SECURITY INVOKER` (Postgres's default, confirmed via `pg_proc.prosecdef = f`) and its body queries `tenant_users` to confirm active membership; `tenant_users` is itself `FORCE ROW LEVEL SECURITY`-protected by a policy that calls `get_current_tenant()` again — infinite recursion, reproduced directly as `psycopg2.errors.StatementTooComplex: stack depth limit exceeded` on a plain `SELECT * FROM products` with `app.current_tenant_id`/`app.current_user_id` both set to a real, valid, active tenant membership | AI/ML (self-found, self-fixed, but this is core RLS/schema architecture — flagging for the schema owner too) | ✅ Resolved 2026-08-27 — `migrations/20260827060000_fix_get_current_tenant_recursion.sql` marks the function `SECURITY DEFINER` (with `SET search_path = public, pg_temp` per standard `SECURITY DEFINER` hardening) so its internal `tenant_users` lookup runs as the function owner — a superuser — bypassing RLS instead of re-entering it. This is the standard, documented Postgres pattern for a helper function that must read an RLS-protected table on behalf of a policy. Verified with a full two-tenant scenario connected as `ceopro_app`: correct per-tenant scoping, fails closed with no context set, a revoked (`removed_at` set) membership loses access, a user can't claim a tenant they don't belong to — all now pass; all 11 new permanent regression tests in `src/ai/tests/test_rls_integration_db.py`. **This means RLS in `Final_schema.sql` was unusable in principle for its actual intended non-superuser role before this fix — not just "not wired up yet" (item #25's gap), but would have crashed on first real use once it was.** |
| 33 | Deep edge-case testing pass (2026-08-27, requested explicitly before merge) beyond the item #27 verification — targeted RLS/tenant-isolation (see item #32), constraint edge cases, cross-tenant data leakage, boundary conditions, and the real Redis cache path (previously only exercised via a `_FakeRedis` test double) | AI/ML (self-tested) | ✅ Complete, all findings resolved or confirmed-safe — beyond item #32: (a) `chk_evidence_shape` correctly rejects a non-forecast evidence row with no category; (b) `chk_review_subject_consistency` correctly rejects `PRODUCT` subject_type with a `competitor_id` also set; (c) `uq_competitor_private` correctly allows the *same* competitor name across *different* tenants (only same-tenant duplicates should collide) — confirmed both directions; (d) a **new permanent test** (`test_load_competitor_prices_stays_tenant_scoped_for_a_shared_global_competitor`) confirms `pricing/data_access.py`'s competitor-price join chain stays tenant-scoped even when two tenants independently track the *same* shared `GLOBAL`-visibility competitor — the trickiest realistic cross-tenant-leak shape, and it holds; (e) `sentiment/`'s and `mpi/`'s data-access queries reviewed directly for the same class of leak — both already correctly `tenant_id`-scoped on every join, no fix needed; (f) MPI's volume-floor comparison guard was missing a boundary test for review_count *exactly equal to* the floor (not just above/below) — added (`test_compare_mpi_results_exactly_at_volume_floor_is_comparable`), confirms `<` (not `<=`) is correct, i.e. exactly-at-floor is comparable; (g) `pricing/guardrails.py::apply_margin_guardrail()` with `cost=0` reviewed — no-ops correctly (floor computes to 0, never exceeds a real positive suggested price), no fix needed; (h) `pricing/data_access.py::_representative_name()`'s JSONB fallback for a product name missing the `"en"` key reviewed — already falls back to any populated language, no fix needed; (i) `extraction/catalog_cache.py` exercised against a **real** Redis container (not `_FakeRedis`) for the full cache-aside cycle — cold path populates + sets a 300s TTL, warm path serves cache without re-querying the DB, `invalidate()` clears the key and forces a fresh read reflecting the DB's current state — all correct, no fix needed. Full live-DB suite after all fixes: 49 passed (up from 37), only the already-tracked item #31 forecasting gap remains. Full offline suite: 170 passed (up from 169), 0 failed. |
| 36 | Full triage of the open PR list (9 PRs, most `CONFLICTING` against `main`), requested 2026-08-27 | AI/ML | ✅ Complete — #16 and #17 rebased clean against `main` (multiple rounds: `main` kept gaining new direct-to-`main` edits, and #14/#18/#19's own merges, throughout this pass; resolving them caught and fixed one genuine content-loss bug a merge itself introduced — see below). #10, #11, #12, #13 closed as superseded (their unique content already landed, in reworked form, in #16/#17 — closure comments on each PR link to exactly where). #15 closed after extracting its still-needed `AI_ENGINEERING_PLAN.md` ([#19](https://github.com/Huda-Hassouneh/CEOPRO-AI/pull/19)); #1 closed after confirming its still-needed `MASTER_SPEC_v4.md`/`AI_PLAN_AND_CONTRACT_UPDATES.md` were already captured in [#18](https://github.com/Huda-Hassouneh/CEOPRO-AI/pull/18). #14's item-numbering collision (its new items #29/#30 vs. this file's own, different, #29/#30) fixed directly on its branch (renumbered to #34/#35) since explicitly authorized to. **A real bug found by this process itself, not just administrative**: merging `origin/main` into #16 auto-resolved a *non-conflicting* hunk (a module docstring in `extraction/data_access.py`) by silently taking `main`'s side, which had dropped the docstring in one of its own direct edits — git reported this merge as clean, but content was still lost. Caught only by diffing the branch's pre- and post-merge state file-by-file rather than trusting "no conflict markers" to mean "nothing changed unexpectedly." Restored in both #16 and #17. **This item's own number changed mid-flight**: originally filed as #34, but PR #14 also landed content at #34/#35 (merged first, via #16's own sync) before this branch's merge caught the second collision — renumbered to #36 here, the next free number once #16/#18/#19's merges are accounted for. |
| 31 | `forecasting/`'s live-DB integration tests (`test_integration_db.py`) fail/error — root cause: `Final_schema.sql` has **no `transactions` table at all** (confirmed: not in any of its 26 `CREATE TABLE` statements). `forecasting/data_access.py::load_daily_demand()` and its evidence/pipeline tests all read from `transactions`, which doesn't exist under the new canonical schema. This is a real, structural gap — not something manual review or `sqlparse` could have caught, since the module was explicitly out of scope for this rework and its code was never touched or re-read against `Final_schema.sql` until this live run exercised it. The closest analog in the new schema is `invoices`/`invoice_items` (line-item `product_id`/`quantity`/`unit_price`/`total_price`, but no per-line `transaction_date` — only `invoices.issue_date` at the invoice level — and no `sale_source`/POS-vs-online distinction), which is a real restructuring, not a 1:1 rename, so this isn't a mechanical fix | Whoever owns the schema architecture decision + AI/ML (forecasting/ owner) | 🔴 Open, new 2026-08-27 — needs a decision, not a unilateral fix: either (1) extend `Final_schema.sql` to bring back a `transactions` table (matching this rework's established "extend, don't reduce" pattern used for items #28/#30 and the earlier `reviews.subject_type` restoration), or (2) deliberately rework `forecasting/` to read demand from `invoices`/`invoice_items` instead — a real design change to how forecasting sources its data, not a rename. Deliberately not decided or fixed as part of this pass, since `forecasting/` was explicitly excluded from the schema-fork rework by prior instruction and this changes what "excluded" can mean once its tests are actually run. **Addendum, found 2026-08-27 while triaging open PRs:** `Final_schema.sql` also has no `model_versions` table at all (`forecasting/evidence.py` writes to it) — a second, independent reason `forecasting/` cannot run against the new schema, compounding rather than duplicating the `transactions` gap above. PR #11 (`claude/rls-app-role-and-migration-fix`) has a small migration for this (`ALTER TABLE model_versions ADD COLUMN artifact_path ...`) but it's moot here since the table itself doesn't exist yet — whatever `transactions` decision gets made, `model_versions` needs the same "extend `Final_schema.sql`" treatment alongside it. |
| 34 | **Recurring pattern of unreviewed direct-to-`main` writes bypassing the PR workflow**, some introducing real regressions — investigated 2026-08-14 after `init_schema.sql` and `migrations/20260807230553_add_market_intelligence_tables.sql` both showed up corrupted/rewritten during a routine merge. Full commit-by-commit table below this one. In short: 26 commits (2026-08-03 → 2026-08-13, not counting normal PR merges) share an identical signature — commit message is exactly `Update <filename>` (GitHub's own auto-filled placeholder when you edit a file via github.com's browser file editor and don't type a custom message, or via the equivalent Contents API call), author is always `Huda-Hassouneh <huda.hasouneh@gmail.com>`, committer is always `GitHub <noreply@github.com>`. Most of these are legitimate incremental edits (adding tables, removing hardcoded credentials, port fixes). Three are not: (a) `cd3ddb3` (2026-08-12 00:50) replaced the entire working 21-table `init_schema.sql` with an unreviewed, breaking 26-table redesign (JSONB multilingual fields, `users.tenant_id` removed) — see item #35 below; (b) `e07e4e4` (2026-08-08 02:25) replaced `migrations/20260807230553_...sql`'s real `CREATE TABLE` SQL with unrelated non-functional Python code — this is the same single incident already logged as item #27 on the (unmerged) `claude/rls-app-role-and-migration-fix` branch, fixed there but never corrected on `main` itself, opened as its own standalone fix: [PR](https://github.com/Huda-Hassouneh/CEOPRO-AI/pull/new/fix-market-intelligence-migration); (c) `cdac377`+`492d9b6` (`watchdog.py`, 2026-08-07) and `444cf2e`+`ad725d9` (`security.py`, 2026-08-08) both show **literal leftover PowerShell here-string/file-write syntax committed as if it were the target file's Python content** (`@'` ... `'@ | Out-File -FilePath ... -Encoding utf8` for watchdog.py; `$content = @' ... '@` + `[System.IO.File]::WriteAllText($targetPath, $content, ...)` for security.py) — strong, concrete evidence of a local PowerShell-based script/automation tool whose own un-executed source text ended up as the committed file content, not a human typing in a browser. `security.py` self-corrected 3 minutes later (`ad725d9`) and is valid Python on `main` today; `watchdog.py` stayed broken (item #16) until fixed manually this session. | Whoever owns write access / any integration with push rights to `main` (Infra/DevOps + repo admin) | 🔴 Open — needs: (1) branch protection on `main` requiring PRs (see item #31's findings), (2) identifying what has direct write access today (GitHub web UI session vs. a PAT/OAuth App/Action — not resolvable from git metadata alone, needs the GitHub organization/repo Audit Log, which this session has no access to) |
| 35 | `init_schema.sql`'s unreviewed 26-table redesign (see item #34, commit `cd3ddb3` and its two follow-ups `f6a49e2`/`0f35506`) is incompatible with the 21-table schema essentially all of `src/ai/`'s existing code was built and tested against (e.g. `products.product_name` as plain text vs. new `JSONB`; `users.tenant_id` direct FK vs. new `tenant_users` pivot table) | Whoever owns the schema architecture decision — needs explicit reconciliation, not a silent merge either direction | 🔴 Open — comparison table requested, not yet reconciled; do not merge either version into the other without an explicit decision |
| 37 | Post-merge professional-QA pass (2026-08-28, requested explicitly after all PRs merged to `main`) — repo-wide integrity/functionality sweep beyond the schema-fork rework's own scope | AI/ML (self-tested) | ✅ Complete, findings below — two real bugs found and fixed, one dependency gap fixed, one significant untested-code gap surfaced and partially closed | Verified clean: no leftover git conflict markers anywhere in the repo; every `.py` under `src/` compiles and AST-parses cleanly (one pre-existing UTF-8 BOM in `src/infrastructure/messaging/ai_consumer.py`, harmless - CPython strips it - not fixed, outside AI/ML scope); no hardcoded secrets/credentials found repo-wide; `docker-compose.yml`'s `migrate` service still coherent with `scripts/apply_migrations.py`; the RLS `SECURITY DEFINER` fix (item #32) verified to also cover a *different* code path not tested during the original fix - `trigger_log_price_change()` (a trigger, not a direct policy check) firing an INSERT into a second RLS-protected table (`product_price_history`) under `ceopro_app` with real tenant context - works correctly, no second recursion bug. **Real bugs found via a static import scan + manual smoke-testing of previously-zero-coverage code** (`git log` confirms none of `template_detection.py`/`row_parsing.py`/`locale_config.py`/`minio_persistence.py`/`ingestion_pipeline.py`/all 5 `adapters/` files had a single test anywhere in the repo before this pass): (a) `sentiment/model.py` imports `torch` directly (`torch.no_grad()`, `torch.softmax()`) but it was never declared in `src/ai/requirements.txt`, only present transitively via `transformers`/`sentence-transformers` - fixed, pinned explicitly per this file's own established convention for exactly this situation; (b) `template_detection.py::_normalize_header()` never folded underscores to spaces, so snake_case headers ("product_name", "unit_price" - `Final_schema.sql`'s own column-naming convention, and the natural shape of anything exported from a database/API) never matched `HEADER_SYNONYMS`, silently degrading to `FALLBACK`/NER extraction instead of the precise `STRICT` typed mapping - fixed, 6 new tests; (c) `row_parsing.py::_parse_typed_cell()`'s "percent" branch never stripped a `%` before calling `normalize_number_string()`, so a `discount_pct` cell written the natural way ("10%", not "10" - how Excel/Sheets write a percent-formatted cell) failed to parse and silently fell through to unmapped/fallback extraction despite the column being correctly identified - fixed, 8 new tests. `locale_config.py` (8 new tests) and `minio_persistence.py`'s document-building logic were checked and found correct, no fix needed - the latter's actual MinIO upload call and `ingestion_pipeline.py`'s DB-touching paths (`_insert_staging_row` etc.) were smoke-tested in dry-run mode only, not covered by permanent tests this pass - **the ~700 remaining lines of this subsystem (all 5 `adapters/`, `ingestion_pipeline.py`'s DB/MinIO paths) are still genuinely untested and this pass found 2 real bugs in the ~3 files it did check - treat the rest as unverified, not as probably-fine.** |

| 38 | Universal Import Engine (spec §12) had a real, structural gap, not just missing tests: none of `extraction/adapters/`'s 5 files (csv/xlsx/pdf/db/api) had any caller anywhere in the repo — every adapter was individually correct but completely unreachable, so "Detect file type" (spec §12, explicit) had no implementation at all. Separately, "Validate values" (spec §12, also explicit) was also missing — a cell that parsed cleanly was treated as fully valid regardless of whether the value made semantic sense (negative price, zero quantity, an implausible date) | AI/ML (self-found, self-fixed) | ✅ Resolved 2026-08-28 — `extraction/file_dispatch.py` (new) dispatches by file extension to the 3 file-based adapters; `row_parsing.py::validate_typed_fields()` (new) rejects negative money/percent/quantity, zero quantity, implausible dates, malformed emails, wired into `ingestion_pipeline.py::process_file()` through the same "report invalid, preserve raw, never silently discard" path a parse failure already used. 53 new tests across a subsystem that had zero coverage before this (`file_dispatch.py`, all 5 adapters, `validate_typed_fields()`, `ingestion_pipeline.py` unit + live-DB). **Still genuinely open, not built this pass**: "Detect currency"/"Detect timezone" per-cell (today's `STRICT`-mode parsing assumes the tenant's home currency/locale from context, a reasonable simplification, not a full per-cell implementation); "Store original files" (the pipeline persists *extraction results* to MinIO, not the original uploaded file itself); "Allow user confirmation" (a UI-layer concern, out of this track's own boundary). |
| 39 | A row whose value Postgres genuinely cannot store (found via live-DB testing: a NUL byte in a JSONB column, `psycopg2.errors.UntranslatableCharacter` — a hard Postgres limitation, not fixable while preserving the raw value untouched) left `ingestion_pipeline.py`'s connection permanently transaction-aborted. Every later statement on that same connection then failed too (`psycopg2.errors.InFailedSqlTransaction`) — every subsequent row's writes, and the final job-count update — silently defeating the module's own documented "one bad row must not sink the whole file" guarantee, for any file where a bad row wasn't the last one | AI/ML (self-found, self-fixed) | ✅ Resolved 2026-08-28 — every DB write in `ingestion_pipeline.py` now runs inside a `SAVEPOINT`/`ROLLBACK TO SAVEPOINT` (`_execute_in_savepoint()`), so one row's DB-level failure only unwinds that row's own write. Verified with a live-DB test: a NUL-byte row followed by a good row — the good row still processes correctly, `conn.commit()` succeeds, job counts land correctly. |
| 40 | Spec S7 states "Pretrained multilingual models must be preferred over training models from scratch" (repeated multiple times in `MASTER_SPEC_v4.md`), and no labeled sentiment training/evaluation dataset exists anywhere in this repo — confirmed, not assumed. Fine-tuning `sentiment/model.py`'s classifier against fabricated data would produce no genuine improvement and a false sense of the task being done | AI/ML (self-scoped) | ✅ Infrastructure built 2026-08-28, genuinely blocked on data, not on missing code — `sentiment/finetune.py`: `load_labeled_dataset()` (strict CSV format, documented in the module's own docstring), `fine_tune()` (HuggingFace `Trainer`, CPU-only defaults, saves locally rather than auto-deploying), and `evaluate_only()` — usable **today**, no training required, to produce real spec §25 metrics (Accuracy, Macro F1, Confusion Matrix) against the current production model. Verified end-to-end against the real model (`AI_TEST_SENTIMENT=1`, ~1.1GB download, matching `test_sentiment_model_real.py`'s existing gate convention) — the full pipeline genuinely runs, not just unit-tested in isolation. **A real dependency gap found while verifying this**: `transformers.Trainer` hard-requires `accelerate`, never previously declared (only `classify()`'s plain inference was covered before) — added to `requirements.txt`. Still open: someone needs to actually produce or source a labeled dataset (`load_labeled_dataset()`'s docstring documents the exact required CSV shape) before either `fine_tune()` or `evaluate_only()` can run against real numbers. |
| 41 | Post-merge integration QA (2026-08-28, testing #38/#39/#40's new code against pre-existing code it shares a dependency with) found `extraction/pipeline.py` (built earlier, unrelated to #38-#40) had the **same class of transaction-poisoning bug fixed in #39, but worse**: `extract_and_store_news_records()`/`extract_and_store_social_mentions()` had no `SAVEPOINT` protection at all, so a DB-rejected entity value (`extracted_entity.entity_value` is `VARCHAR(512)` — a long catalog-matched product/competitor name realistically exceeds that) poisoned the connection, and the `except` block's own recovery write (marking that record `'Failed'`) then *also* failed on the poisoned connection — an uncaught exception that crashed the entire function, not just that one record, abandoning every other record in the batch (including perfectly valid ones) at `'Pending'` forever with no error surfaced anywhere | AI/ML (self-found, self-fixed) | ✅ Resolved 2026-08-28 — each record's extract-and-persist now runs inside a `SAVEPOINT`, rolled back to (not the whole transaction) on failure before the recovery write runs, so that write is guaranteed to execute against a clean connection. Reproduced directly against a live Postgres before the fix (confirmed crash: `psycopg2.errors.InFailedSqlTransaction`, a second valid record left stranded at `'Pending'`); verified after the fix that the same scenario now returns normally, the bad record is marked `'Failed'`, and the good record is correctly marked `'Processed'`. New permanent regression test added alongside the existing live-DB extraction suite. |
| 42 | `data_sources.connection_credentials_vault` (plain `TEXT`, defined in `Final_schema.sql` since before this session, never previously read anywhere) is now actually used, wired in 2026-08-31 as the credential source for the new `google_places`/`amazon_paapi` collectors (`data_access.py::load_source()` → `cli.py` → spider `credentials_json`) — used pragmatically as a JSON blob of raw API credentials (`{"api_key": ...}` / `{"access_key", "secret_key", "partner_tag"}`), since no secrets-manager integration exists anywhere in this repo despite the column's "vault" name implying one. This means Google/Amazon API keys sit in this column as plaintext in an ordinary `TEXT` column, same protection level as any other row — no field-level encryption, no external vault/KMS reference | Whoever owns secrets-management architecture (Infra/Security) | 🟡 Open, flagged not silently assumed fine — functionally correct and RLS-scoped like every other `data_sources` column, but a real secrets manager (or at minimum application-level encryption of this column) would be the right fix before real API keys are ever put in it for production tenants. Not built this pass — a secrets-manager integration is new infrastructure, not a bug fix, and out of this track's own scope to decide unilaterally. The one adjacent gap that *was* closed (2026-09-07): the only way to actually write this column used to be a raw `SQL UPDATE` by hand — `policy_cli.py set-credentials` (`--credentials`/`--credentials-file`) is now the real, tested path, and `--credentials-file` avoids the secret ever touching shell history. Still plaintext at rest either way — this doesn't move the encryption/vault architecture question below. |
| 43 | Live extraction test (`mocks/Electronics_For_Test.xlsx`, 51,947 rows, 2026-08-31) found a real DB-transaction scaling cliff: the full file took **8.5 hours** (1.7 rows/sec) through the live pipeline vs. **7 seconds** (7,424 rows/sec) for the identical rows with `conn=None` (CPU-only, no DB) — a ~4,400x gap traced to `ingestion_pipeline.py` running ~104,000 `SAVEPOINT`s (2 per row) inside one uncommitted transaction for the whole file, a known Postgres degradation pattern once subtransaction count grows past the small in-memory cache. | AI/ML (self-found, self-fixed) | ✅ Resolved 2026-08-31 — `process_records()` gained an opt-in `commit_every` parameter (default `None`, preserving the original all-or-nothing transaction for callers that rely on it, e.g. `test_extraction_ingestion_pipeline_integration_db.py`'s own rollback-based cleanup); `POST /extraction/upload` now passes `commit_every=500` (env `EXTRACTION_COMMIT_EVERY`) on every real upload. Re-ran the exact same 51,947-row file with the fix: **3.65 minutes (237.3 rows/sec), a 140x speedup**, DB-side job counts verified to exactly match the in-memory summary via a second connection. New regression tests prove both the batching behavior and that the unset default is unchanged. Full writeup in `src/ai/extraction/SCALING.md`; bulk writes and horizontal parallelization (fix plan items #2/#3) remain open for when they're actually needed. |
| 44 | Same live test found a `HEADER_SYNONYMS` gap: a real POS export's headers (`Sale_ID, Date_Time, Product_ID, Product_Name, Quantity, Unit_Price, Total_Price, Shift`) matched only the 3 required fields (`product_name`/`quantity`/`unit_price`) before this fix — 3/8 = 0.375 coverage, below `RECOGNIZED_MODE_MIN_COVERAGE` (0.5) — so every one of the 51,947 rows fell to weak `FALLBACK` regex/NER extraction (0 typed fields captured per row) despite `Total_Price`/`Date_Time` being unambiguous real-world spellings of `amount_raw`/`transaction_date` | AI/ML (self-found, self-fixed) | ✅ Resolved 2026-08-31 — added `"total price"` and `"date time"`/`"datetime"` to `HEADER_SYNONYMS` (`template_detection.py`), now CONFIRMED (not ASSUMED) against a real POS export. Re-ran the full 51,947-row file (CPU-only, no DB) after the fix: mode correctly resolves to `RECOGNIZED`, `total_fields_expected == total_fields_extracted == 259,735`, genuine `data_loss_pct = 0.0`. New regression test `test_real_pos_export_shape_matches_recognized_mode` added. |
| 45 | Rigorous audit of `src/ai/rag/` (2026-08-31, hybrid FAISS+BM25+RRF retrieval) found the entire module could not run against the real schema at all: (a) `data_access.py`/`pipeline.py` queried `rag_documents_metadata.minio_object_key`, which doesn't exist — the real column is `storage_bucket_path`; (b) worse, found only while implementing the fix and running it live: `processed_status` (the whole Pending/Processed/Failed document lifecycle this module's ingestion depends on) doesn't exist **anywhere** in `Final_schema.sql` or any prior migration — not a rename, a genuinely missing column, confirmed via `UndefinedColumn` against a real disposable Postgres before writing the fix. Separately, `seed_demo_data.py`'s RAG-seeding block had 3 more bugs found in passing while already touching it: missing `file_size_bytes` (NOT NULL, no default), wrong chunk-text column name (`chunk_text` vs `chunk_text_content`), and a 1024-dim random vector against a `vector(384)` column. Also found: `rag_document_chunks.embedding` (a `pgvector` column added by an earlier, separately-tracked migration, item #1) was never read or written by any code — every retrieval call re-fetched every tenant document from MinIO, re-chunked, and re-embedded the entire corpus from scratch, on every question asked. And: no re-ranking stage existed despite spec `MASTER_SPEC_v4.md`'s own RAG workflow listing "OPTIONAL RERANKING" as a stage (Cross-encoder reranking is separately listed under Phase 7 - Advanced System, i.e. deliberately deferred, not an oversight). | AI/ML (self-found, self-fixed) | ✅ Resolved 2026-08-31 — `storage_bucket_path` fixed across `data_access.py`/`pipeline.py`/test fixtures/`seed_demo_data.py`; new migration `20260831000000_add_rag_document_status_column.sql` adds `processed_status` (+ CHECK + index); `seed_demo_data.py`'s other 3 bugs fixed in the same pass. `data_access.py` gained `replace_document_chunks()` (idempotent delete-then-insert persistence to `rag_document_chunks`, no new Python dependency — embeddings sent as a `%s::vector`-cast bracketed literal, parsed back from pgvector's text representation by hand) and `load_tenant_chunks()`; `pipeline.py`'s `ingest_pending_documents()` now chunks+embeds+persists once at ingest time, `build_tenant_index()`/`build_hybrid_index()` read persisted chunks (no MinIO fetch, no re-embedding — signatures simplified accordingly, `minio_client`/`bucket` params dropped since retrieval no longer touches MinIO at all); `retrieve_hybrid()` widens the pre-fusion candidate pool per retriever (`max(top_k*4, 20)`, not just `top_k`) so RRF isn't starved of candidates before fusion. New `reranking.py`: multilingual Cross-Encoder (`cross-encoder/mmarco-mMiniLMv2-L12-H384-v1` — deliberately not the more common English-only `ms-marco-MiniLM-L-6-v2`, spec §8's Arabic-English requirement applies here too) re-ranks the RRF-fused pool down to the caller's real `top_k`; a re-ranking failure falls back to RRF order rather than failing the request. New `pipeline.run_retrieval()`/`assemble_context()` compose the full pipeline into one call returning an `AssembledContext` (context text + source citations) — the LLM reasoning step is deliberately not this module's concern (still evaluating hosting providers), so `run_retrieval()` is the complete, LLM-agnostic handoff point. 25 new tests (offline: pgvector literal round-trip, re-ranking logic via dependency injection, context assembly; live-DB: persistence, wipe-and-replace idempotency proven directly — not assumed from the `DELETE` in the SQL — full `run_retrieval()` end-to-end with a real reranker, graceful reranker-failure fallback). Full offline suite clean (358 passed); live-DB RAG suite clean against a fresh disposable Postgres+MinIO with every migration applied. |
| 46 | Found in passing while verifying `seed_demo_data.py`'s RAG fixes (item #45) end-to-end: an unrelated, pre-existing section of the same script — `currency_rates` seeding — has the identical class of bug (wrong column names: `base_currency`/`target_currency`/`rate`/`rate_date` used in the INSERT vs. the real schema's `from_currency`/`to_currency`/`exchange_rate`/`last_fetched`). Confirmed directly: running the script against a real disposable Postgres fails immediately with `UndefinedColumn` on this INSERT, before ever reaching the RAG section. Fixing it forward (requested explicitly, "so the whole script runs cleanly") surfaced **three more, independent bugs further into the same script**, each only reachable once the previous one was fixed: (a) `_seed_tenant()`'s user-creation block inserted `tenant_id`/`role` directly into `users`, which has neither column — multi-tenancy and role assignment both go through the separate `tenant_users` pivot table; (b) the `products` INSERT passed a plain string for `product_name`, which is `JSONB` (multilingual, `{"en": ...}` shape), not text — failed with `invalid input syntax for type json`; (c) the `inventory` INSERT used `ON CONFLICT (product_id)`, but `inventory` has no unique/exclusion constraint on `product_id` alone (only `inventory_id`, the real PK) — failed with `there is no unique or exclusion constraint matching the ON CONFLICT specification`. None of these four bugs could have been found by inspecting the script alone; each was hidden behind the previous one and only surfaced by actually running it | Whoever owns `seed_demo_data.py` (Infra/DB, per item #19's precedent) | ✅ Resolved 2026-09-01 — all four bugs fixed in sequence, each re-verified by re-running the script against the same live disposable Postgres until it completed cleanly, not assumed fixed from reading the diff. Confirmed idempotent: running it twice in a row leaves correct, non-duplicated counts (2 companies, 6 products, 2 `tenant_users`, 3 `currency_rates`). Full repo test suite re-run clean afterward (370 offline, 69 live-DB, 0 failed) against the same Postgres instance the seed data was left in, confirming the seeded rows don't interfere with anything. |

### Item #34 detail — full commit-by-commit history

All 26 commits sharing the `Update <filename>` / author=Huda-Hassouneh / committer=GitHub signature, on `main`, 2026-08-03 → 2026-08-13 (PR-merge commits excluded — those are normal). "Verdict" is based only on content this session has actually diffed and either previously investigated or checked while compiling this table — entries marked *not audited* carry no claim either way, just a flag for future review.

| # | Time (+03) | Commit | File | Verdict | Current status on `main` |
|---|---|---|---|---|---|
| 1 | 08-03 00:39 | `1185b9b` | `docker-compose.yml` | ✅ Correct — replaced hardcoded Postgres/MinIO passwords with `${...}` env var references | Stands, still correct |
| 2 | 08-06 21:15 | `a93dcf1` | `src/infrastructure/init_broker.py` | ⚪ Not audited this session | Unreviewed |
| 3 | 08-07 07:55 | `041d7e0` | `init_schema.sql` | ✅ Correct — added `evidence_records`, `recommendation_outcomes`, `data_sources`, `ingestion_jobs`, `model_versions`, `audit_logs` tables (all part of the schema `src/ai/` was later built against) | Stands, still correct |
| 4 | 08-07 07:57 | `a5030ad` | `.env.example` | ⚪ Not audited this session | Unreviewed |
| 5 | 08-07 07:58 | `78b216d` | `docker-compose.yml` | ✅ Correct — port mapping normalization (`5435:5432` → `5432:5432`) | Stands, still correct |
| 6 | 08-07 07:59 | `6efdc9c` | `src/infrastructure/security.py` | ✅ Correct — added required-`JWT_SECRET`-at-startup check, no hardcoded fallback | Superseded by #17 below |
| 7 | 08-07 07:59 | `5cbe877` | `src/infrastructure/check_connectivity.py` | ⚪ Not audited this session | Unreviewed |
| 8 | 08-07 08:00 | `010d97d` | `DATA_OWNERSHIP_AND_CONTRACTS.md` | ⚪ Not audited this session (docs-only, low risk) | Unreviewed |
| 9 | 08-07 11:51 | `a62e895` | `src/infrastructure/messaging/token_rotator.py` | ⚪ Not audited this session | Unreviewed |
| 10 | 08-07 11:56 | `8ae0434` | `src/infrastructure/messaging/ai_consumer.py` | 🟡 Partial check — this specific edit added the correct `DATABASE_URL`-required pattern; the credential-fallback regression item #23 (elsewhere in this file) later found in this same file was **not** introduced here, so it must trace to a later, unaudited change | The #23 bugs were found and fixed 2026-08-08 (this session) |
| 11 | 08-07 11:57 | `e2fcc3a` | `src/infrastructure/messaging/producer.py` | ⚪ Not audited this session | Unreviewed |
| 12 | 08-07 12:36 | `07891bb` | `src/infrastructure/database/backup_verifier.py` | ⚪ Not audited this session | Unreviewed |
| 13 | 08-07 12:41 | `03956e9` | `src/infrastructure/messaging/app_consumer.py` | ⚪ Not audited this session | Unreviewed |
| 14 | 08-07 12:43 | `5ec39c6` | `src/infrastructure/Dockerfile.backend` | ⚪ Not audited this session | Unreviewed |
| 15 | 08-07 13:19 | `cdac377` | `src/infrastructure/monitoring/watchdog.py` | 🔴 **Bug introduced** — replaced the module docstring's opening `"""` with a literal PowerShell here-string opener `@'`; the file's own trailing `'@ \| Out-File -FilePath ... -Encoding utf8` (added by this same commit) is unmistakably leftover PowerShell script syntax, not Python — strong evidence of a local PowerShell-based write script whose own source leaked into the committed file instead of being executed | Item #16 — fixed manually this session (2026-08-08), restored proper `"""` quoting |
| 16 | 08-07 17:42 | `b8cbed9` | `init_schema.sql` | ✅ Correct — added `pgvector` extension, `product_price_history`, `reviews`, `sentiment_results`, `rag_document_chunks`, `currency_rates`, `import_staging_rows` tables | Stands, still correct |
| 17 | 08-07 18:57 | `22d90b4` | `init_schema.sql` | ⚪ Empty commit — zero content difference from its parent (`b8cbed9`); GitHub's web editor produces exactly this when a file is opened and re-saved with no real edit | No-op, nothing to fix |
| 18 | 08-07 20:00 | `492d9b6` | `src/infrastructure/monitoring/watchdog.py` | 🟡 Partial fix — removed the PowerShell `@'` opener and the trailing `'@ \| Out-File ...` line from #15's damage, but never restored the actual `"""` triple-quotes, so the file remained invalid Python | Fixed alongside item #16, same session |
| 19 | 08-07 23:16 | `55ae05e` | `PENDING_ACTIONS.md` | ⚪ Not audited this session (self-edit of this tracking file) | Unreviewed |
| 20 | 08-08 02:18 | `444cf2e` | `src/infrastructure/security.py` | 🔴 **Bug introduced** — file began with the literal line `$content = @'`, i.e. a PowerShell variable assignment opening a here-string, followed by what should have been the file's real Python content | Fixed 3 minutes later by #22 |
| 21 | 08-08 02:20 | `b042661` | `.github/workflows/staging-deployment.yml` | ⚪ Not audited in detail — large diff (230 lines removed), previously noted in `AI_PROGRESS.md` as fixing hardcoded secrets while leaving the missing-files problem (item #8) unchanged | Partially reviewed in an earlier session pass |
| 22 | 08-08 02:21 | `ad725d9` | `src/infrastructure/security.py` | ✅ Self-corrected — removed the leftover `$content = @'` opener and the trailing PowerShell `[System.IO.File]::WriteAllText(...)` / `Write-Host` block #20 left behind. Confirmed via `py_compile` this session: the file is valid Python on `main` today | Fixed, stands correct (one code smell noted in passing, not confirmed as a bug: a bare `except Exception: pass` around token-rotation validation) |
| 23 | 08-08 02:25 | `e07e4e4` | `migrations/20260807230553_add_market_intelligence_tables.sql` | 🔴 **Bug introduced** — replaced the real `CREATE TABLE news_record/social_mention/extracted_entity` SQL with an unrelated, non-functional Python `get_tenant_connection()` helper | Still broken on `main` today — standalone fix opened, see item #34 above |
| 24 | 08-12 00:50 | `cd3ddb3` | `init_schema.sql` | 🔴 **Breaking, unreviewed redesign** — replaced the entire 21-table schema with a new 26-table one (JSONB multilingual fields, `users.tenant_id` removed, new `tenant_users`/`system_roles` RBAC tables) | Still on `main` today — see item #35, comparison requested, not reconciled |
| 25 | 08-12 01:17 | `f6a49e2` | `init_schema.sql` | Continuation of #24's redesign (formatting cleanup + one constraint tightened) — same open question as #24 | Still on `main` today |
| 26 | 08-13 13:40 | `0f35506` | `init_schema.sql` | Continuation of #24's redesign (added `system_roles`, `system_alerts`, `audit_logs`, expanded several other tables) — same open question as #24 | Still on `main` today |

**What git metadata cannot answer**: whether these commits were made by the account holder directly through github.com's browser editor, or by an automated script/integration using that account's credentials or an API token. Both produce identical author/committer/GPG signatures. The PowerShell-artifact commits (#15, #20) are the strongest evidence pointing toward local script automation specifically (a human pasting into a browser text box is unlikely to include `[System.IO.File]::WriteAllText(...)` boilerplate), but this is inference, not proof. The only source that could resolve this definitively is GitHub's organization/repository **Audit Log** (Settings → Audit log), which records actual originating session type (web / OAuth App / PAT name) and IP — this session has no access to it.

## Waiting on documents / approvals (not code)

| # | Item | Owner | Status |
|---|---|---|---|
| 11 | `AI_ENGINEERING_PLAN.md`, `AI_CONTRACT_CHANGES_AND_CLARIFICATIONS.md`, and the original `AI_PROGRESS.md` referenced by `AI_PLAN_AND_CONTRACT_UPDATES.md` (module-level detail, "Module E/J/K" references) don't exist anywhere in this repo's git history | Whoever authored `AI_PLAN_AND_CONTRACT_UPDATES.md` | 🟡 Open — acknowledged as "will provide later"; AI/ML work in the meantime is going directly off `MASTER_SPEC_v4.md` |
| 12 | `MASTER_SPEC_v4.md` and `AI_PLAN_AND_CONTRACT_UPDATES.md` are staged locally / pushed to `noorhassouneh-patch-1` but not yet merged to `main` (reported as pending approval, pusher lacks `main` permissions) | Whoever holds merge rights on `main` | 🟡 Open, PR raised 2026-08-27 — during a triage of the open PR list (9 PRs, most conflicting against `main`), confirmed these two files still weren't on `main` and nothing else open carried just them cleanly. Opened [PR #18](https://github.com/Huda-Hassouneh/CEOPRO-AI/pull/18) with exactly these two files (byte-identical to `noorhassouneh-patch-1`'s copies), isolated from the rest of PR #1's now-stale infra content. Still needs an actual merge from someone with `main` permissions. |
| 13 | Orange infrastructure environment (CPU/GPU availability, on-prem vs. cloud) not yet confirmed (spec §28) | Whoever owns the Orange deployment relationship | ⚪ Deferred — confirmed CPU-only / no GPU "for now" for this track; revisit if/when GPU becomes available |

---

## How to update this file

- Resolving an item: change its Status cell (e.g. `🔴 Open` → `✅ Resolved 2026-08-10 — <one-line note, link to commit/PR if applicable>`). Don't delete the row.
- New item: add a row to the relevant section (or add a new section if it doesn't fit the existing three). Keep the Owner column specific enough that someone reading it knows who to ping.
- If an item turns out to already be resolved elsewhere, still record it here rather than silently dropping it — same reasoning as `AI_PLAN_AND_CONTRACT_UPDATES.md`'s precedence rule: the record matters even after the fact.
