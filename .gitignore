__pycache__/
*.pyc
.pytest_cache/
.env
.venv/
venv/
data/market/

# Local LLM model files (scripts/download_local_llm_model.py) - multi-GB
# binaries, never belong in the repo. Same reasoning as data/market/ above.
models/
*.gguf

# Stray Node.js build output/dependencies with no package.json anywhere in
# this repo (this backend is Python-only) - environment artifacts, not
# source, never belong in a Python service's git history.
node_modules/
dist/
