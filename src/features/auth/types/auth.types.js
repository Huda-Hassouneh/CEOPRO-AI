export const authStatuses = Object.freeze([
  'initializing',
  'unauthenticated',
  'authenticated',
]);

export const authRoles = Object.freeze([
  'admin',
  'manager',
  'editor',
  'viewer',
]);
