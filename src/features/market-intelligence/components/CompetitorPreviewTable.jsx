import Badge from '../../../shared/components/ui/Badge.jsx';
export function CompetitorPreviewTable({ t, rows, onDetails }) { return <div className="market-table-wrap"><table className="market-table"><thead><tr><th>{t('market.competitors.name')}</th><th>{t('market.competitors.sentiment')}</th><th>{t('market.competitors.price')}</th><th>{t('market.competitors.activity')}</th><th>{t('market.competitors.updated')}</th><th>{t('market.competitors.actions')}</th></tr></thead><tbody>{rows.map((row) => <tr key={row.name}><td><span className="market-entity-logo" style={{ background: row.color }}>{row.name.charAt(0)}</span><strong>{row.name}</strong><small>{row.domain}</small></td><td><Badge variant={row.tone === 'positive' ? 'light-success' : row.tone === 'negative' ? 'error' : 'neutral'}>{t(row.sentimentKey)}</Badge></td><td><span className="market-score"><i style={{ width: `${Number.parseFloat(row.price) * 10}%` }} />{row.price}</span></td><td><Badge variant={row.activityKey === 'market.status.high' ? 'light-success' : 'warning'}>{t(row.activityKey)}</Badge><small>{t(row.activityKey2)}</small></td><td>{t(row.updatedKey)}</td><td><button type="button" className="market-table-action" onClick={() => onDetails?.(row.name)}>{t('market.competitors.viewDetails')}</button></td></tr>)}</tbody></table></div>; }
