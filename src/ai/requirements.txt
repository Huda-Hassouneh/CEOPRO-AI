xgboost>=2.0,<3.0
pandas>=2.0,<3.0
numpy>=1.24,<2.0
# Never import sklearn directly - this is here because xgboost.XGBRegressor
# (model.py) hard-requires it internally and raises ImportError without it,
# confirmed by testing in a venv with xgboost but no scikit-learn installed.
scikit-learn>=1.3,<2.0
psycopg2-binary>=2.9,<3.0
redis>=5.0,<6.0
rank-bm25>=0.2,<0.3
minio>=7.2,<8.0
sentence-transformers>=3.0,<4.0
faiss-cpu>=1.8,<2.0
# XLM-RoBERTa-based sentiment classifier (spec S16) - transformers is already
# a transitive dependency of sentence-transformers, pinned here explicitly
# since src/ai/sentiment/model.py imports it directly. sentencepiece and
# protobuf are XLM-RoBERTa's slow-tokenizer requirements - without them,
# AutoTokenizer.from_pretrained fails on this model.
transformers>=4.40,<6.0
sentencepiece>=0.2,<0.3
protobuf>=4.25,<8.0
pytest>=7.4,<8.0
